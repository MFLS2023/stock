# 交易知识库

> 项目根目录：`C:\Users\20577\Documents\炒股\知识库`
> 索引数据库：`_知识库系统\indexes\knowledge.db`
> 最后更新：2026-08-24（**全面修复轮**：① OCR 引擎切 RapidOCR，南京路/郁金香/波段之门付费层用 dual 双引擎逐图择优，
> 坏块率大幅下降且可信片段保留率复核通过；② 检索四项速赢——预览截断 420→1200、同形词降噪闸门、兜底重切四个
> 触发条件、`--expand` 默认开，同义词表升 v6；③ 结构修复——sources.yaml 整文件回写清零、select_sector_strong
> 静默降级修复、三公式补测试、复利杯闲聊标记 22 块并排序降权。当日实测全库 **18991 块** / 7 来源。
> ⚠️ 历史上出现过的 19047 / 18951 / 18875 等都是中间态，以现测为准）
> 上一次：2026-08-23（方向校准：第一定位改为一问一答问答库，方法卡降级为可选副产品，kb-ask 成为默认入口；
> `_导师试验` 全面审查）
> ⚠️ **本文件里出现过的 18913 / 18875 / 18901 / boduanzhimen 8187 / nanjinglu 551、568、587
> 都是中间状态，已不成立。** 以 `build_index.py` 输出和 `SELECT source_id,COUNT(*)` 为准：
> 2026-08-09 18:0x 实测全库 **18951**（kongkonglong 172、郁金香 OCR 重跑 1181→1213），
> boduanzhimen **7986**，nanjinglu_bian **586**。
> 2026-08-23 再测：全库 **19047**（boduanzhimen **8082**，其余六源未变）—— 又漂了，继续以现测为准。
> 这一天有两个会话并行改库，南京路被重导过四次（551 → 568 → 587 → **586**，
> 后三次是 OCR 策略 v2/v3/v4 迭代，见下方「OCR 重做」一节），
> 波段之门两次（15:12 → 8187 中间值、15:52 → 7986）。**块数是移动靶，别拿文档里的
> 历史值做断言**，要现测。
> 出现偏差的原因是同一天有两个会话并行改库（一个补波段之门付费 PDF 的 OCR、
> 一个接入第 7 个来源），前者写文档时后者的 jsonl 还没落定。
> **数字对不上时一律重跑 `build_index.py` 现测，不要相信文档里的历史值。**
> 上一次：2026-08-09（南京路彼岸新增《风起云涌》+ OCR 策略重做，525 → **586 块**）
> 上一次：2026-08-09（**第 6 个来源「波段之门」7169 块已接入**，全库 17869 块；同义词表升 v4 补波浪理论词汇域）
> 上一次：2026-08-09（修 `MIN_AUTHOR_REPLY` 丢掉 78% 作者回复的缺陷，回复 14,136 → 48,840 条；爱在冰川 SKILL v1.0 建成）
> 上一次：2026-08-08（导师 SKILL 三个实测问题：.claude 副本是 v1.0 旧版、frontmatter 字段名错、引文多数不可回溯）
> 上一次：2026-08-08（方法卡 20 张审批通过进索引，16 张附用户未解问题；测试基线回到 SPEC 冻结值）
> 上一次：2026-08-08（爱在冰川图文关联：`image_path` 打通，3237 块可回看原图；`chunks` 表加列）
> 上一次：2026-08-05（郁金香 SKILL 升 v2.0，17 个问题全修；新增 14 条阈值是 AI 推的，待用户认）
> 再上次：2026-08-05（郁金香对抗测试完成，17 个问题待修；「子项目：_导师试验」一节同步）
> 再上次：2026-08-05（新增「子项目：_导师试验」一节 —— 此前整个子项目在主文档零引用）
> 再上次：2026-08-04（接入第 5 个来源 `aizaibingchuan`；修复 build_index.py status 过滤 + source_name 硬编码；methods.jsonl 加 status 字段；validate_kb.py 加一致性校验）

---

## 这个系统在做什么

**把 7 位交易员的经验语料做成一问一答的问答库**：用户在对话框里问交易问题，
AI 查库后用人话回答，并说清是谁说的、出自哪篇。这是本项目的第一定位
（2026-08-23 方向校准确立，用户原话：想要"类 ima 那种一问一答的知识库"）。

**核心指标是问答质量，不是方法卡数量，也不是可检索记录数。**
衡量口径：真实问题测试集上「能据此正确作答」的比例，以及回答中的引用是否可回溯。
基线（2026-08-23 实测，12 题来自方法卡未解问题）：8 能答 / 2 半答 / 1 不能答 /
1 能答带同形词噪声，原始证据在 `reports/_demo1_raw.txt`；2026-08-24 检索修复后重跑
（`_demo1_raw_v2_20260824.txt`）：带噪声案例清零、截断复原、其余不劣于基线。
改动检索或 skill 后重跑对比。

**方法卡已降级为可选副产品（2026-08-23 用户决定）：**
- 已审的 20 张保留在索引里——Demo 1 实测它们是问答场景的第一命中资产，一张不动
- 不再主动生产新卡；只有用户明确说「帮我记下来 / 做成卡」时才走 tutor 流程
- 旧的「AI 蒸馏草稿 → 用户逐张审批」分工不再是日常流程

---

## 运行环境（两个解释器，各缺一样东西）

本机有两个 Python。**2026-08-24 起 Python312 是事实上的全能解释器**（yaml/numpy/pandas/
requests/PIL/docx/pypdf/rapidocr 全有，全套 225 项测试在它上面跑），codex 运行时
曾在 08-07 被重置、pip 包全丢（yaml/requests 都没有了），别再默认它什么都能跑：

| 用途 | 解释器 | 状态 |
|------|--------|----|
| **测试、导入器、建索引、校验、查询、OCR（全用这个）** | `C:\Users\20577\AppData\Local\Programs\Python\Python312\python.exe` | ✅ 全能 |
| codex 运行时 | `~/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe` | ⚠️ 08-07 重置后只剩标准库；其 `native/poppler` 二进制仍可用且被 `_bench_ocr.py` 引用 |

**上表漏了一条：Python312 有 `python-docx`**（2026-08-09 实测 `import docx` 成功）。
所以纯 docx/txt 的来源（如 `kongkonglong`）用 Python312 就能跑完导入+建索引全流程，
不必切到 codex 运行时。两个解释器都缺 `olefile`，旧版 `.doc` 只能走 Word COM
（`scripts/convert_doc_to_docx.ps1`，实测可用）。

⚠️ **Windows 子进程读 UTF-8 输出必须显式传 encoding**：子进程默认按 GBK 解码
`--json` 的 UTF-8 流会直接解析失败（2026-08-24 审计实测踩到）。所有 `subprocess.run`
读 Python 输出的地方都要带 `encoding="utf-8"`。

⚠️ **写新导入器不要在顶层 `import pypdf`。** `import_boduanzhimen.py:40` 就这么写的，
结果它在 Python312 上直接 `ModuleNotFoundError`，连里面不涉及 PDF 的函数都没法复用。
没有 PDF 的来源根本不需要它；有 PDF 的按 `register_source.py:72` 的做法在函数内 import。

- `yaml` 和 `requests` 是 2026-08-08 补装到 codex 运行时的（`pyyaml==6.0.2`、
  `requests 2.34.2`）。装之前全套 `discover` 会因 `import yaml` / `import requests`
  各报一个 error，而那两个 import 并非本轮改动引入 —— 是既有未提交改动带的。
  补装后**全套 187 项第一次做到裸 OK、零 error**。
- **装依赖前必须先清代理**：本机 `HTTP_PROXY=http://127.0.0.1:7897` 指向的代理
  已不通（`curl` 返回 000），pip 会卡在 ProxyError。做法：
  `unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy && export NO_PROXY="*"`。
  这个死代理也会让 `urllib`/`requests` 抓网页失败，**失败不等于对方站点有问题**。
- 所有脚本都要 `export PYTHONIOENCODING=utf-8`，否则中文输出乱码或 `UnicodeEncodeError`
  （Windows 默认 GBK，写 JSON 遇生僻字会直接崩）。

## 目录结构

```
知识库\
├── 复利杯\                        ← 原始资料，只读
├── 南京路彼岸\                    ← 原始资料，只读
├── 郁金香花园付费文章文档版\      ← 原始资料，只读
├── 飞书聊天记录_潘凤\             ← 原始资料，只读（已 gitignore，体积大）
├── 爱在冰川\                      ← 原始资料，只读（2655 个 .md，87 MB）
├── .agents\skills\                ← 六个项目级工作流（见下方 Skills）
├── _导师试验\                     ← 子项目：导师数字人格提取（隔离，可整体删除）
└── _知识库系统\
    ├── config\                    ← sources.yaml / taxonomy.yaml
    ├── source_libraries\          ← 每个正式来源一个目录，目录名 = source_id
    ├── indexes\knowledge.db       ← SQLite FTS5
    ├── personal\trading_journal\  ← 个人日志（私密，不混入来源）
    └── scripts\                   ← 导入/索引/查询/验证脚本
```

**只读原则：原始来源文件夹内的文件不得覆盖、改名、移动或写入派生内容。**

**来源清单以 `_知识库系统\config\sources.yaml` 为唯一事实来源。** 上面的目录树会过时，
登记表不会。原始目录名不必等于 `source_id`（`panfeng` 的原始目录是
`飞书聊天记录_潘凤\`，载体名保留原样），但 `source_libraries\` 下的目录名必须等于
`source_id`——`build_index.py` 靠这个识别来源。

---

## Skills（工作流入口）

| 场景 | 触发方式 |
|------|---------|
| **问任何交易问题（默认入口）**：概念是什么 / 某人怎么看 / 某情况怎么判断 / 某说法对不对 | 直接问，不用敲命令 —— AI 自动走 `/kb-ask`：查库 → 读块 → 先答案后出处 |
| 学习一个概念，生成方法卡草稿（仅在用户说「帮我记下来/做成卡」时用） | `/trading-knowledge-tutor` |
| 跨来源对比同一概念 | `/cross-source-synthesizer` |
| 核验某个观点是否有当前市场证据 | `/market-evidence-verifier` |
| 查行情该调哪个接口（盘中实时 + 盘后复盘） | `/market-data-toolbox` |
| 复盘一笔交易，对照方法卡 | `/trading-journal-reviewer` |
| 导入新来源，执行预检和注册 | `/trading-source-curator` |
| 用某位导师的视角推演盘面 | `/nanjinglu-bian-perspective`、`/yujinxiang-perspective`、`/boduanzhimen-perspective`、`/aizaibingchuan-perspective` |
| 潘凤短线体系联网决策（九步决策链，输出出手/不出手 + 仓位上限，**非人格推演**） | `/panfeng-trading` |

**这些斜杠命令在 2026-08-08 之前是敲不出来的。** 六个工作流放在 `.agents/skills/`
（Codex 的目录），而 Claude Code 只扫 `~/.claude/skills/` —— 两边不通，
所以用户在 Claude Code 里一个都调不了，只能手敲 `python query_kb.py`。
已用 `scripts/sync_skills.py --apply` 同步过去。同步源现为 **12 个**
（`.agents/skills/` 7 个 + `_导师试验/skills/` 5 个），加上 HOME 原生的 6 个
（codex-collaboration、doc-toolkit、fetch-community、grill-me、industry-chain-researcher、nuwa-skill），
Claude Code 现可见 **18 个 skill**（2026-08-23 `ls` 实数，随增删变化）。
格式本来就兼容（都用 `name:` 首字段，无 Codex 专有字段），纯粹是位置问题。

---

## 当前已接入的来源

**索引实测 18991 块 / 7 个来源**（2026-08-24 核实，OCR 引擎切换重跑后；以现测为准）：

| id | display_name | 索引内块数 | 载体 | 批准日期 |
|----|--------------|------|------|---------|
| `boduanzhimen` | 波段之门 | **8029** | 公众号 md + 付费扫描 PDF（OCR） | 2026-08-09 |
| `aizaibingchuan` | 爱在冰川 | **7338** | 微信公众号 Markdown + 9 篇 PDF/docx 汇编 | 2026-08-04 |
| `fulibei` | 复利杯 | 1470 | 语音转写文字稿 | — |
| `tulip_garden` | 郁金香花园 | **1212** | Word + 截图 | — |
| `nanjinglu_bian` | 南京路彼岸 | **584** | PDF 文章 | — |
| `panfeng` | 我有上将潘凤 | 186 | 飞书聊天记录 HTML | 2026-08-02 |
| `kongkonglong` | 主升龙头空空龙 | **172** | docx/txt 方法论长文 | 2026-08-09 |

⚠️ **上表的 boduanzhimen 曾被记成 8187，那个数字不成立。** 它来自一次尚未落定的
中间状态：15:12 那次重导入把 `article_body` 写到 2110，随后 15:52 又重跑了一次
（付费 PDF 的 OCR 补充），最终 jsonl 是 **7986**（`article_body` 1909）。
同一天两个会话并行改库时，文档记的数字可能比 jsonl 旧 —— **以现测为准**。

**本文件下文多处仍写着 9175 / 9155 / 5813 / 17869 / 7169 / 525，那些是历史值。** 变化叠加：

1. **爱在冰川 5813 → 7338**：修 `MIN_AUTHOR_REPLY`（25 → 3）找回被误丢的短回复，
   见下方「MIN_AUTHOR_REPLY」一节
2. **新增第 6 个来源 `boduanzhimen`（波段之门）**：849 篇，
   **时间跨度 2012-01 至 2026-07，14 年**，是全库跨度最长的来源
3. **波段之门 7169 → 7986**（2026-08-09，两次重导入，最终 15:52 落定）：
   `article_body` 1092 → 1909，其余四类（author_reply 4984 / paid_article 542 /
   chart_ocr 532 / indicator_formula 19）一字未变（**当时口径**；08-24 OCR 引擎
   切换重导后现测为 4984 / **531** / **586** / 19，与下方表格一致）。
   中途曾出现 8187 那个中间值，见上方警示
4. **南京路彼岸 525 → 551**（2026-08-09 15:25）：新增《风起云涌》2026-08-09（12 页，
   9993 字，26 块 = 正文 12 + 截图 OCR 14）。四节：医药主线、超预期即弱转强、量价、主线画像
5. **新增第 7 个来源 `kongkonglong`（主升龙头空空龙）172 块**：14 个 docx/txt 里
   10 篇建块、4 篇是冗余（登记不建块）。见下方专节

复利杯 1470 = transcript 1444 + 方法卡 20 + 分歧卡 6（20 张卡已于 2026-08-08 审批进索引）。

### 波段之门的三个特殊之处

| 项 | 值 | 说明 |
|---|---|---|
| `author_reply` | **4984** | 比正文块（`article_body` 1909 + `paid_article` 531）还多 |
| `chart_ocr` | **586** | K 线图内文字，引擎切 RapidOCR 后 628→586、汉字量持平且坏块率归零（下方专节） |
| `indicator_formula` | **19** | **全库唯一带可执行指标公式的类型** |

**⚠️ 不要把它当成「波浪理论」的库。** 实测词频：`波浪` 64 块、`艾略特` 22、
`推动浪` **0**；而 `周期` 295、`结构` 213、`均线` 173、`变盘` 86。
他的主干是**时间周期 + 自拟指标 + 多周期结构**，浪型只是顺手借用的描述词。
他本人对纯结构派明确不屑，原话（`[2025-05-20] 今年有没有三浪三`，留言区回复）：

> 除了李彪，我没见一个用缠论说对大行情时间的，没有时间的推论，等于没说。
> 有时间，不知道具体空间，是绝对能够赚钱的。有空间，没时间，这绝对是忽悠人的。

用户可能因为看他常说「三浪三」而要求「用波浪理论分析」，**先纠正这一点再回答**，
否则会拿错工具。

与另五个来源的短线情绪体系几乎不重叠，跨来源比较时**问错词会得到假结论**：
`情绪周期` 他 **0** 块（其他五源 338）、`连板` 8（1177）、`接力` 6（877）；
反过来 `浪` 他 580（413）、`变盘` 86（141）。问他「情绪周期」要改问「时间周期/变盘」。
独有术语：`浪形` 18 块（其他五源 0）、`背驰` 13（0）、`副图` 6（0）。
这批词已补进 `config/synonyms.yaml`（v4 四组 + 2026-08-09 新增的 7 组口语问法）。

#### 它独有的三个工具（2026-08-09 建）

其他来源只有语料，这个来源多了**可执行**的东西 —— 因为他的方法能落到公式上。

| 脚本 | 干什么 | 验证状态 |
|---|---|---|
| `scripts/boduanzhimen_indicators.py` | 15 个通达信公式的 Python 实现 | 23/23 测试通过 |
| `scripts/boduanzhimen_backtest.py` | 抽显式点位预测，用真实上证日线核对 | 17/17 测试通过 |
| `scripts/test_mutation_boduanzhimen.py` | **变异测试**：故意改坏阈值看测试能否发现 | 7/7 全抓住 |

**为什么有变异测试**：2026-08-09 实测发现「18 个测试全绿」是**虚假信心** ——
把 `kdj_battle` 的阈值 15.5 改成 15.0、`AND` 改成 `OR`、布林带 2σ 改成 1.5σ、
翻倍选股涨停线 1.099 放宽到 1.05，**7 个变异一个都测不出来（0/7）**，
因为那批测试只断言「列名齐全」「三态互斥」这类恒真条件。
补了阈值级双向断言后才 7/7 全抓住。**改动指标或测试后必须跑这个脚本**，
否则不知道测试是真在守还是只是绿着。

指标翻译的两个坑（有测试守着，别改回去）：
通达信 `SMA(X,N,M)` 是递推加权 `(M*X+(N-M)*Y')/N`，等价 `ewm(alpha=M/N, adjust=False)`，
写成 `ewm(span=N)` 是错的；`STD` 是**样本**标准差 `ddof=1`。
KDJ 用东财上证 60 根真实数据做基准，跳过前 20 根让递推收敛后逐根比对，
最大偏离 K=0.069 / D=0.333。

**回测数字的正确读法**（29 条可检验）：55.2% / 69.0% / 63.0%（20/60/120 日窗口）。
2026-08-09 晚修完时态判定缺陷后重跑，旧数 28 条 / 50.0% / 64.3% / 60.0% 已作废 ——
旧判据按整段检测，点位后面带过去式词的假预测也被计成命中。明细见
`_导师试验/skills/boduanzhimen-perspective/SKILL.md` 的回测一节。
**这三个数本身就是结论 —— 涨的是窗口长度，不是他的能力，单报一个数就是误导。**
另有 7 条日内点位用 1/3 日判，不能塞进同一张表（用 60 天窗口测当日阻力位，
测的是「这两个月指数涨没涨」）。旧报告 `reports/boduanzhimen_预测回测.md` 已作废
（72 条 / 63.9%，口径过宽含 15% 假预测），文件头有警示，保留它是因为
「同一批语料换口径能差 2.6 倍」本身是最好的例证。

#### 图片：K 线图形读不出来，只能靠公式重算

3718 张 K 线图全部 OCR 过，**只有 628 张（约 17%）值得入库**
（下表是旧一轮按 532 张口径的分桶明细，比例结构仍可参考；
最新数字以 `_导师试验/skills/boduanzhimen-perspective/SKILL.md` 为准）：

| 结果 | 张数 | 原因 |
|---|---:|---|
| 入库 `chart_ocr` | 532 | 他写在图上的判断，正文往往没有 |
| 识别为空 | 913 | 纯 K 线，无文字 |
| 文字太短 | 832 | 只有水印或零星数字 |
| 中文占比过低 | 1180 | 数字刻度、板块代码 |
| 行情面板 | 249 | 「A股成交B股成交国债成交…」字段名连排，中文比高但无语义 |

**图形信息（哪条线是 70 日均线、黄线有没有上穿红线）一张都读不出来** ——
OCR 原理上做不到。替代路线是**用公式重算**：他的图都是通达信按公式画的，
公式已翻成 Python，拿真实数据算一遍比认他的旧截图可靠。
入库那 628 张的价值在于图上的文字，例如「3949 已破 3936 是第一道防线」。
⚠️ **OCR 数字可能被误认**（`4034` vs `4D34`），关键点位必须回看原图。

平均每篇 7.4 张图（K 线等实质图 5.2 张，共 5283 张 / 715 篇）。
⚠️ 图片扩展名不可信：4214 张 PNG 命名为 `.jpg`，`maps/image_map.jsonl`
的 `real_format` 记按文件头判定的真实格式。

#### 两个数据陷阱

1. **`market_cache.db` 和 `live-market` MCP 的 `000001` 都是平安银行（11 元）不是上证指数。**
   取上证指数用 `stock_data` MCP 的 `index_prices(symbol="000001")`，
   或用 `data/sh000001_daily.csv`（6000 根，2001-11~2026-08）。
   这坑踩过两次，现在 `boduanzhimen_backtest.py` 会校验：收盘价中位数 < 500 直接报错。
2. **付费包里混有别人的文章。** `250223+瑞+这波行情的规律`（9 块，
   `boduanzhimen-paid-8b768011e090`）文件名里的「瑞」是原作者。
   词频证据：`分时承接` 6 块**全在该文**、其余 723 篇 0 块；而他的标志词
   `周线级别` 在该文 0 块（其余 17 块）。检索命中它时要说明作者不是他。

已接入的来源**彼此同级，没有主次**。跨来源检索时逐个查询、分别记录，不合并。
新增来源走 `/trading-source-curator`，必须先 `--dry-run` 再由用户批准，
批准日期记入 `sources.yaml` 的 `approved_on`。

以下各节按来源列出各自的形态和已知问题。

### 复利杯（fulibei）

语音转写的直播/连麦文字稿，多人，有说话人和时间戳。
干货密度低，闲聊多（前 10-15 分钟属于正常）。
有价值的部分是一线交易员实战思路（闻少、曾一六等）。

**⚠️ 已知问题（截止 2026-08-01；2026-08-24 更新）：**
- `claim_type` transcript 主体固定为 `opinion_or_case`；方法卡 rule 20 / 分歧卡 opinion 6 有区分
- `confidence` 全部固定为 `medium`，不可用于过滤
- ~~闲聊段落未标记~~ **已修 2026-08-24**：22 块标 `chunk_type=chitchat` 并在排序层 -3
  （`mark_fulibei_chitchat.py`，人工逐条核对；仍可检索）。开场寒暄不再抢 top 位
- ASR 同音字多：竞价/竟价，顺灏/顺浩同学

---

### 南京路彼岸（nanjinglu_bian）

单作者连续方法论文章，体系最完整，是概念学习的主干来源。
"弱转强"、"情绪周期"、"趋势对抗"等核心概念有完整推演。

**✅ 抽取方式已修复（2026-08-02）：改为文本层 + OCR 双路保留**

这批 PDF 是「文章正文（有文本层）+ 嵌入行情截图（只能 OCR）」的混合页，
择一使用必然丢内容。实测「文本层优先，择一」方案会让 75 页正文减少约 1 万字，
因为截图内容被丢掉了。现在同一页产出两类块，各自标注可信度：

### OCR 重做（2026-08-09，v1 → v4）：改走 PDF 嵌入图对象

**块数 525 → 586，正文字符 213765 → 241927。** 三层现状（表外字率 = 该层汉字里
不在文本层字表 1968 字内的比例，越低越干净）：

| chunk_type | confidence | 块数（08-24） | 表外字率* | 内容 |
|-----------|-----------|-----|---------|------|
| `article` | high | **252** | 0.0% | 文本层正文，完全干净 |
| `article` | medium | **104** | 5.8%* | 无文本层页的正文，OCR 承载 |
| `screenshot_ocr` | low | **228** | 8.6%* | 截图内容，仅此层可得 |

\* 表外字率是 08-09 引擎切换前测的；08-24 换 RapidOCR+dual 后坏块率
35%/29% → 17%/14%，受灾片段保留率复核通过（见「OCR 引擎」一节）。

原来整页渲染成 PNG 再 OCR，现在**直接提取页面里的嵌入图像对象**
（`kb_import_utils.extract_page_images`）。关键实测：

1. **页面嵌的图本身是低分辨率位图**（1080×584、878×364、665×460 这种量级）。
   渲染整页到 300 DPI 等于把 1080px 的源**放大 2.3 倍**，插值模糊让中文笔画断开 ——
   OCR 把「你」读成「亻尔」、「强」读成「忄虽」、「能」读成「自旨」。
   这解释了为什么**提高 DPI 反而更差**：140 DPI 加权还原率 40.2%，300 DPI 只有 31.9%。
2. **但「原生分辨率」也不对**。只提图不放大时逐页判定是**变差 34 页 / 变好 20 页**，
   分界线是图宽：变差的都是 660-666px 的图，变好的是 1080px。665px 的图在
   1157px 宽的页面渲染里本来是被放大的，直接用原生尺寸就丢了这个放大。
   补上 `target_width=1600` 的 LANCZOS 放大后：那批页表内字率 57.4% → 89.5%，
   大图无反伤（86.0% → 88.2%）。
3. **无文本层的页（78 页）正文也改用嵌入图**。这些页正文本身在图里，
   嵌入图放大到 1600 比 140 DPI 页面栅格更清楚（六页实测 89.1% → 94.5%，
   字数 1215 → 1364）。取胜条件是「图层字数 ≥ 渲染字数 × 0.9」，
   防止用干净的碎片换掉完整的正文。

有可用文本层的页**不再需要 n-gram 减法** —— 正文和截图来自不同对象天然分离。
那个减法只是为了消除整页渲染造成的重叠，且实测减不干净：旧版曾把 7014 字
正文糊重复留在截图层（`情绪周期在产业趋势投资中的应用` 一篇就有 6413 字）。

对 v1 逐页判定（分层看）：

| 层 | 变好 | 变差 | 持平 |
|---|---:|---:|---:|
| 正文层 `article` | **24** | 1 | 43 |
| 截图层 `screenshot_ocr` | 35 | 18 | 51 |

正文层那 1 页是 28 字图注碎片。截图层 18 页全部核对过：5 页是读到更多内容
（表外率被数字拉高），13 页的旧内容在新版其他层有更完整的干净版。

**⚠️ 剩余无法改善的部分**：行情软件截图里的**数字密集表格**（129 块，表外率 11.3%）。
板块代码 `880401 880729`、价格序列、竖排股票名，字号极小，任何路径都读不准。
中文为主的块（111 块）表外率 4.9%。引用数字前必须回看原页。

回答时优先用 `high`，引用 `low` 块必须标注 `⚠️ OCR低置信度` 并建议回看原页。

**最新一篇：《风起云涌》2026-08-09**（12 页 / 9993 字 / 26 块，全部零错误，
正文 12 块全 `high`，无 `medium` —— 这篇文本层完好）。四节内容：

1. **医药主线** —— 7 月整月股灾后医药在 50 日 RPS 领先，「趋势的内生动力不以主观意志转移」，
   回指《核心：趋势对抗》
2. **超预期即弱转强** —— 「弱应该更弱，强应该更强」，哈药周四尾盘放量大跌是「弱的超预期」，
   周五 9:33:28 五分钟内爆量封死是「强的超预期」；情绪「点（龙头）→线（板块）→面（全市场）」逐级扩散
3. **量价** —— 有量无价=滞涨，有价无量=加速（风险聚集）；
   **明确否定「接分歧」**：「接分歧本质上也是错的……应该接分歧转一致」
4. **主线画像五角色** —— 破局龙万邦 / 趋势主升龙哈药 / 转点卡位百花 / 弹性龙陇神 /
   趋势中军药明、凯莱英、康龙

**文件名的 `(1)` 已在 2026-08-09 去掉**：原文件是 `2026-08-09风起云涌(1).pdf`（浏览器下载
重名标记），库内标题一度是 `风起云涌(1)`，引用出处带这尾巴。已经用户批准后改名为
`2026-08-09风起云涌.pdf` 并重导入 —— **改名不触发重新 OCR**，因为 `doc_id` 和页缓存 key
都基于文件 sha256，内容没变缓存全命中（实测 7 秒跑完，551 块 / 213765 字符逐位不变）。

`clean_title()` 只剥日期前缀，不动尾部括号，这是故意的：`2025总结篇(2)` 那个 `(2)`
是真的第二篇，一刀切会误伤。下载重名标记只能改文件名解决。

---

### 郁金香花园（tulip_garden）

付费课程，Word 正文 + 大量截图，涵盖筹码、竞价、仓位。

**✅ 切块过碎已修复（2026-08-02）：**
连续短段合并为 400-800 字的块，图片 OCR 块保持独立。

| 指标 | 修复前 | 修复后 |
|------|-------|-------|
| 块数 | 6393 | 1181（2026-08-09 OCR 重跑后 1213） |
| 中位数 | 19 字 | 410 字 |
| 短于 50 字 | 77.8% | 6.9% |

内容逐字符校验无丢失（430541 字符）。合并块的 `locator` 是段落区间
（如 `正文第49段—正文第57段`），原始段号保留在 `merged_locators` 字段。

剩余 6.9% 短块中，图片说明占 68%，正文列表项占 30%（被相邻截图隔断，
展开 Parent 可读到上下文）。

**OCR 放大（2026-08-09）：截图 OCR 前放大到 2000px 宽**，块数 1181 → **1213**，
字符 430541 → 486128，`image_ocr` 层表内字率 93.3% → **94.2%**（OCR 汉字 +3059）。

这是南京路那轮 OCR 重做（见上文）的副产品，但**收益小得多，是优化不是修复**：
郁金香的截图本来就是独立图片文件，中位宽度已 1080px，不像南京路那些
660px 的嵌入图那样吃亏。分带实测（42 张抽样）：

| 图宽带 | 原样 | 放大到 2000px |
|---|---|---|
| <800px | 88.2% / 812字 | 89.9% / 956字 |
| 800-1200px | 91.3% | 91.6% |
| >=1200px | 95.0% | 96.1% |

三带都改善但都在 1-2 个百分点。留下是因为代价只有每张图一次 resize
（`OCR_CACHE_VERSION = 2` 触发重算，全量约 11 分钟）。

⚠️ **`extract_page_images` 那条主要改动用不上这个来源** —— 它没有「PDF 嵌入图对象」
可提，图片本来就是独立文件。真正可移植的只有放大这一条。

---

### 我有上将潘凤（panfeng）

盘中聊天记录，飞书 HTML 导出。载体是聊天记录，来源身份是作者本人——
换成微信或语音转写，来源还是同一个人。所以 `source_id` 用作者身份命名，
而 `chunk_type: feishu_chat`、`adapter: specialized_feishu_chat` 保留载体名，
描述的是结构和解析能力。

| 项 | 值 |
|---|---|
| 原始 HTML | 1 个，16 个消息组，3445 条消息 |
| 发言人 | 我有上将潘凤 3326 条、5280 共 119 条 |
| 日期范围 | 2026-06-23 至 2026-07-31，29 个有消息的交易日 |
| 产物 | 29 个每日文档 / 60 个父块 / 186 个检索块 |
| 图片、外链 | 均为 0 |
| 定位方式 | 日期 + 时间 + 消息编号（`primary_locator: date_time_message_number`） |

**⚠️ 已知特性（不是缺陷，检索时需知道）：**

- **块数少一个数量级**（186 vs 1470/525/1181），且是口语而非术语。
  实测 `弱转强` 0 条、`龙头` 2 条、`竞价` 1 条、`半路` 16 条。
  查不到很常见，如实报"未找到"，不要硬凑
- **日期部分是结构化推断**：部分组头只有时分没有年月日，导入器从绝对日期锚点
  出发、在组内时间回退 90 分钟以上时推进到下一个周一至周五（共推断 23 次）。
  规则与全部锚点闭合，但**不等同于飞书原生时间戳**，日期敏感的结论要回看原文
- **原文有规避写法**：拼音、希腊字母、同音字替代证券名称。标准化只做 Unicode、
  空白和冗余签名清理（剔除尾部 `【潘凤老师】` 1047 条），**不猜测恢复证券名称**
- **`unknown` 时间继承 2 条**：飞书导出里时间缺失的消息继承上一条的时间
- 全部内容标为历史观点或案例，不作为当前市场事实

---

### 爱在冰川（aizaibingchuan）

微信公众号导出 Markdown，单作者，2017-06 至 2026-08 跨 10 年，库内体量最大的来源
（5813 块，占全库 63.4%）。主体是每日盘后复盘与次日计划，另有周末随笔和方法论文章。
另有 9 篇第三方汇编（PDF/docx）单独入库，见下方「额外资料」小节。

| 项 | 值 |
|---|---|
| 原始 .md | 2655 个，87 MB（原始目录 `爱在冰川\`，只读） |
| 产物 | 2537 文档 / 5184 父块 / 5813 检索块 / 4,094,344 字符 |
| 块长 | 最短 35，中位 669，均值 704，最长 1601 |
| 定位方式 | 正文按段号（`正文第12段—正文第18段`），作者回复按留言序号（`留言区第1条—第8条`） |
| 跳过文档 | 127 篇整篇未入库（见下方说明，用户 2026-08-04 决定不救） |

| chunk_type | 块数 | 内容 |
|-----------|-----|------|
| `qa_reply` | **3901** | 留言区里作者本人的回复，与提问配成「问：…答：…」（2026-08-09 修 `MIN_AUTHOR_REPLY` 后从 2376 增至 3901，见下方说明） |
| `daily_review` | 1653 | 每日盘后复盘 |
| `daily_data` | 944 | 每日数据小节（涨停动因、赚钱效应、各种数据） |
| `article` | 616 | 方法论文章 |
| `weekend_essay` | 175 | 周末随笔 |
| `curated_digest` | 36 | 第三方摘录/语录汇编，**不是作者原文**（见额外资料小节） |
| `course_ocr` | 13 | 付费课程讲义扫描件 OCR，误识多 |

**2023 三季度空洞已补齐（2026-08-04 增量导入）。** 用户补下载了缺失的 `.md`，
源端新增 882 个文件里按同一套规则（`azbc_delta_new.py` 复用 `azbc_pick.py` 的 classify）
筛出 **71 篇入库**（每日数据 59 / 鱼块 11 / 其他正文 1），剔除资讯汇总 621 + 资金流 107
+ 无正文 83。产物增量：documents +71、parents +79、chunks +131，新增块噪声检查
（图片语法/裸链接/粗体/读者留言/免责行/表情占位符/locator 格式）**全部 0 异常**。

补完后 2023-07-01~09-30 的 65 个工作日里只剩 7 天无内容（`07-06`、`07-13`、`07-19`、
`09-04`、`09-19`、`09-27`、`09-29`，末者中秋休市），作者本就不是每个交易日都发文，
**不再算空洞**。全时段剩余 22 段连续空白（≥3 工作日）逐段核对为国庆/春节/劳动节/中秋，
真正非节假日的只剩 `2017-07-14~19`、`2024-06-25~27`、`2024-10-18~22` 三段。

**127 篇跳过文档到底是什么**（2026-08-04 实测更正，此前文档写成「只留作者回复」是错的）：

跳过条件在 `scripts\import_aizaibingchuan.py:575`：正文清洗后中文不足 100 字
**且**留言区无作者回复 —— 两个条件同时成立才跳过，且是**整篇跳过、连 document 都不生成**。
实测 `documents.jsonl` 2537 篇全部有 chunk，零 chunk 文档 0 篇，印证了这一点。

「靠作者回复入库」是**另外 97 篇**（正文<100 字但 `author_reply_count`>0），
这 97 篇全部入库了。两者是不同的集合，别混为一谈。
127 篇的逐篇清单见 `爱在冰川数据导入任务包\06_待你确认_127篇清单.md`
（A 组 30 篇作者实写 ≥60 字、B 组 31 篇 20-59 字、C 组 66 篇 <20 字）。

**用户 2026-08-04 决定：不救。** `MIN_DOC_CHARS` 保持 100 不下调。
所以这 127 篇的内容永久不在库内，问到对应日期会「查不到」——
其中 30 篇作者实写 ≥60 字（如 `2024-08-28 数据`），需要时只能回看原文。
2023 三季度增量导入后跳过数仍是 127，说明新补的 71 篇一篇都没落进跳过条件。

**有意丢弃的内容：**

- 读者留言 7,710,825 字符全部丢弃，只提作者本人回复（**1,164,831 字符，48,840 条**）

### 🔴 2026-08-09 修了一个丢掉 78% 作者回复的缺陷

`MIN_AUTHOR_REPLY` 原为 **25**，把短于 25 字符的作者回复整条丢弃。
抽样 120 篇实测：作者回复 2562 条，只入库 561 条（21.9%），**丢掉 2001 条（78.1%）**。

**被丢的不是噪声。** 25 个汉字对中文是一整句话，这个阈值本意滤「嗯」「是的」，
实际滤掉了他最直接的回答：

| 被丢的回复 | 是什么 |
|---|---|
| 「能封住的板，明天都应该有溢价」（14字） | 交易规则 |
| 「1进2难选所以才性价比高啊」（13字） | 赔率判断 |
| 「理论上是有，但是就怕走成了省广」（15字） | 条件判断带具名对照 |
| 「看情况，暂时不打算卖。。。」（13字） | 他的实际持仓决策 |
| 「5，不适用了」（6字） | **对自己战法当期适用性的否定** |

最后那条是发现这个缺陷的引子：调研 agent 报告「他对法系列一回过『不适用了』」，
我查全库 `instr(text,'不适用了')` 只有 1 块且与法系列一无关，追问后才发现
它在原文里、但没进库。

**真噪声不是「短」，是楼层报数**：`答：「2」←问：「5」`、`答：「1楼」`、`答：「3楼」`。
按内容模式排除（`FLOOR_REPLY_RE`），与长度无关。`MIN_AUTHOR_REPLY` 降到 3，只挡空串和单字。

修复后的影响：
- 作者回复 14,136 → **48,840 条**（3.45 倍）
- `qa_reply` 块 2376 → **3901**
- 爱在冰川总块数 5813 → **7338**
- 跳过文档 127 → **55**（72 篇原本「正文短且无回复」被整篇丢弃的文档现在有回复了）
- 全库 9175 → 10674（当时口径，之后 `boduanzhimen` 接入又变）
- **三来源冻结样本完全未受影响**（1470 / 525 / 1181），隔离有效
- 资讯汇总 621 篇、短线资金流 107 篇、无正文 83 篇未纳入（用户明确要求），合计剔除 811 篇。
  验算：源端 3466 个 `.md` = 入库 2655 + 剔除 811
- 微信样板、图片语法、裸链接、表情占位符共占原文约 77%，已清洗

**⚠️ 已知问题（截止 2026-08-04）：**

- **公众号正文里的图片未 OCR**：原文大量行情截图、持仓截图，图表**文字内容**一律不在库内。
  `risk_flags` 已标注，结论涉及图表必须回看原文。
  （例外：额外资料里的课程讲义做了 OCR，见下方小节）
  **但图片位置已可定位**：2026-08-08 起 `image_path` 字段能直接指到本地图片文件，
  见下方「图文关联」小节。也就是说图里的字仍搜不到，但搜到正文后能一键看到对应的图。
- **`date` 是发布日，不是交易日**：取自文件名方括号。2026-08-04 增量导入后重测
  （`azbc_recount3.py`，规则：标题里 `20YY`+2~4 位数字能**唯一**切出合法日期才计，
  切分有歧义或一个标题里出现多个日期都不计）：2528 篇 `.md` 里可判定 1855 篇、
  歧义 210 篇、无日期 463 篇。可判定的 1855 篇中 **1496 篇与发布日同日、
  323 篇发布日晚 1 天**（盘后复盘发在次日凌晨）、14 篇晚 2 天、
  10 篇差 89~1096 天（**全是作者把年份打错**，如《2018年的第一个交易日201711爱在冰川复盘》
  发布于 2018-01-01 而标题写成 2017）。日期敏感的结论要核对标题里的日期。
  这批数字比旧版记录的「402 篇带交易日」多，因为旧版正则窄，漏掉了 `202372数据`
  这类无分隔符写法，不是数据变了
- **作者对持仓打码**：原文按「阅读上 2w 就打马赛克」的惯例隐去证券名，
  原文里就已缺失，库内同样没有，**不猜测恢复**
- `claim_type` 全部固定为 `opinion_or_case`（5813 块）、`market_regime` 全部「未标注」，
  两者都是导入器写死的常量，不可用于过滤
- `confidence` 只在额外资料上有区分：公众号正文 5764 块全为 `high`（写死的常量，
  **不代表评估结果**），额外资料 36 块 `medium` + 13 块 `low`（按素材性质实标）。
  换句话说，拿 `confidence=high` 筛这个来源等于筛掉那 49 块第三方汇编，筛不出"更可靠的正文"
- `topics` 有约 1.4% 的块为空，属词表未命中，与 panfeng 的 42.5% 同类，不是缺陷

**该来源独有说法**（其他四来源零命中，检索时单独试，括号为 2026-08-04 增量导入后重测块数）：
`涨停动因` 1354、`各种数据` 1420、`方向展望` 385、`北向资金` 267、`填权` 178、
`业绩报` 151、`川神` 709、`股池` 5、`长上影` 20、`瞎扯淡` 181、
`极度低迷` 3、`空间龙` 5、`无人关注` 1。
另有 `借楼` 963 —— 这是作者在留言区回复的固定开头（「答：借楼一用：…」），
是载体特征不是交易术语，用它检索等于筛出全部留言回复。

#### 图文关联 image_path（2026-08-08 建立）

作者大量用「上图/下图/图中/箭头」指代配图（全库 **860 处**指代），但导入器
`clean_wechat_markdown` 把图片语法整体删除，正文里不留痕迹 —— 读者看到「下图是天汽模
今天的分时」却拿不到那张图。现在 `chunks.image_path` 能直接指到本地图片文件。

| 项 | 值 |
|---|---|
| 映射覆盖 | 2529 篇文章 / 23865 张图 |
| 关联到块 | **3237 块**（占公众号正文 5764 块的 56%），23309 张图 |
| 含指代的块覆盖率 | **86.0%**（345/401） |
| 路径失效 | 23983 个引用 **0 个失效** |
| 生成器 | `scripts\build_azbc_image_map.py`（`--verify` 跑命中率检验） |
| 中间产物 | `source_libraries\aizaibingchuan\image_map.json` |

**图片本体不在本项目内**，在 wechatDownload 的下载目录
`C:\Users\20577\Neverflandre\wechatDownload4.6\下载\爱在冰川\图片\`（35878 个文件 / 2.5 GB）。
换机器或移动该目录后必须重跑映射生成器，否则 23983 个路径全部失效。

**图片分类（md5 去重实测）**：净内容 24338 张（67.8%）、模板页脚 8504（23.7%，
重复最多那张出现 **6770 次**、73% 在文章尾部）、封面 2780（7.7%）、文内重复 256。
`封面\` 目录 3428 个文件去重后只有 **535 张唯一图**。
`短线资金流\`（4117 文件 / 477 MB）是顶层独立目录，从未进入图片池。

**位置精度 59.6%，不是 95%，别当精确定位用。** 判据是「作者说图的地方紧邻是否真有图」，
对照组（不含指代的普通段落）25.1%，**提升 2.38 倍**，所以位置信息真实有效但有噪声。
两种对齐策略分开看：可推导的「封面偏移」命中 65.5%（653 篇），
按数量猜的「尾部对齐」58.2%（1876 篇）。另有 283 篇（11%）段落未能全部回溯到原文偏移。

**段号必须与 locator 同源。** 生成器直接 import 导入器的 `clean_wechat_markdown` /
`split_body_and_comments` / `strip_leading_title`，不自己重写清洗逻辑。
**踩过的坑**：最初想「在正文里插图片占位符再跑管线」，实测占位符把纯图片行由空行变成
非空行，`strip_leading_title` 等的判断随之改变，**1154 篇段号漂移**。改为管线原样跑、
位置用原文字符偏移另算，才对上。

**`图片\` 子目录按标题建而非按文章**：作者周末系列每周同名，`肘墨鱼块` 一个目录里
1354 张图跨 **142 个日期**，`ZMYK` 234 张跨 26 个日期。所以归组必须用文件名里的
日期+时间，不能用目录名。

**留言区块（`qa_reply`，现 3901 块）不关联图片**，留言区没有作者配图。
⚠️ 2026-08-09 修 `MIN_AUTHOR_REPLY` 后 `qa_reply` 从 2376 增至 3901，
但 `image_map.json` 未重新生成 —— 图文关联只覆盖正文块，不受影响。

#### 额外资料（9 篇第三方汇编，49 块）

不是公众号导出，是另外收来的 PDF/docx，放在 `爱在冰川\额外资料\`（只读），
由 `scripts\import_azbc_extras.py` 单独导入（重跑幂等）。
**其中 8 篇不是作者本人发布的内容**，`author` 字段已写明整理者，
检索结果里必须按 `author` 区分，不能当成导师原话引用。

| 素材 | 篇 | 块 | chunk_type | confidence | author |
|------|---|---|-----------|-----------|--------|
| 干货文章点集（股不南摘录） | 7 | 31 | `curated_digest` | medium | 股不南（读者摘录，非作者原文） |
| 语录及感悟（STALK 整理） | 1 | 5 | `curated_digest` | medium | STALK 整理（非作者原文排版） |
| 高送转战法付费课程讲义 | 1 | 13 | `course_ocr` | low | 爱在冰川（课程讲义，OCR） |

- 日期：干货点取文件名前缀（2023-06-22 ~ 2023-10-13）、课程讲义取 PDF `/CreationDate`
  （2018-11-09）、语录取 docx `created`（2023-09-21）。**都不是交易日**
- 干货点与库内原文有 3-8 成重复（探针实测每篇 40 条抽样命中 7~30 条），
  检索时可能同一观点出现两次，一次是原文一次是摘录
- **课程讲义 OCR 全 24 页收录，只丢了末页**（中文不足 40 字）。
  **没有按「正文页 / 行情截图页」分流** —— 试过六个指标（中文率、数字率、英文率、
  句读密度、常用字率、最长连续中文串），两类页面取值区间全部重叠，最优组合也只有
  「正文留 6/11、截图误留 3、正文误删 5」。根因是截图页 OCR 出的不是垃圾：K 线批注、
  行情表里的股票名都是真内容。所以同一块里可能正文与乱码混排，
  例如 `与其代码相近的东音股份（002793），开盘顶一反抢筹。后续涨幅最大 38%` 之后
  紧跟 `蘸 2 0 5 % ) 肝 2 ( 3 1 3 的 %`。**数字（价格、日期、涨幅）尤其不可信**，
  任何结论必须回看原页
- `source_summary.json` 由主导入器写、只统计 .md 那批，额外资料的总数补记在
  该文件的 `extras_included` 键里。**重跑 `import_aizaibingchuan.py` 会整文件覆盖
  jsonl 并抹掉这 49 行，之后必须再跑一次 `import_azbc_extras.py`**

---

### 主升龙头空空龙（kongkonglong）

单作者方法论长文，讲的是一件很窄很具体的事：**早盘 9:30-9:45 的超预期首板，
怎么不被炸板爆头**。库内体量最小（172 块，占全库 0.9%）但词汇域完全独立。

| 项 | 值 |
|---|---|
| 原始文件 | 14 个（11 docx + 2 txt + 1 doc），5.1 MB |
| 产物 | 14 documents / 32 parents / **172 chunks** |
| 建块文档 | **10 篇**（另 4 篇是冗余，登记但不建块） |
| 块长 | 中位 566 字，最短 168、最长 995（原始段落中位仅 57 字，75.9% 短于 100 字） |
| 定位方式 | 原始段号（`正文第12段`），超长段切片加「之N」后缀 |
| 导入器 | `scripts/import_kongkonglong.py`（约 640 行） |

| chunk_type | 块数 | 内容 |
|-----------|-----:|------|
| `method_article` | 167 | 方法论长文，无具名个股、大量规则句，可长期复用 |
| `market_commentary` | 5 | 盘面点评，具名个股 + 具体日期，**不可作为通用规律引用** |

#### 这个来源带来一整个此前零命中的词汇域

接入前实测这批词在 17869 块里**全部零命中**，现已可检索（已进 `synonyms.yaml` v5，
括号内为 2026-08-09 16:31 实测全库命中块数）：

`防爆头` 25、 `虚假封单` 30、 `虚假骗炮` 22、 `伪超预期` 16、 `超预期首板` 10、 `换手生死线` 10、 `筹码生死线` 8、 `套牢盘生死线` 6、 `千亿资金` 3、 `横切` 3、 `外切` 2、 `反压位` 2、 `围炉取暖` 1

**低频不等于无效** —— `围炉取暖` 只有 1 块，但它是「资金反复抱团同一细分板块」
这个概念在全库唯一的表达。他的「生死线」是一套量化阈值命名法
（如「套牢盘占流通盘超 15% 直接放弃早盘秒板」）。

#### 四篇不建块的文档及判据

判据是**段落级语义近似**（`difflib` ratio ≥ 0.75，标点空白归一化后，段长 ≥15 字），
不是逐字比对 —— 逐字会把「暴利」→「正反馈」这类合规改写误判成独有内容。

**对照集必须是「全部已建块文档」，不是单一目标**，这一点决定结论：
`防爆头2.0` 只对 `20260418` 算时独有 2434 字（16.4%，超门槛），
对 `20260418` + `新日期拆解` 两篇一起算才降到 1576 字。

| 文件 | 处置 | 实测判据 |
|---|---|---|
| `防爆头2.0.docx` | superseded → `20260418` | 段数口径下被 `20260418` 覆盖 **89.2%~91.0%**（随段长门槛变化；字数口径 76.5%~83.5%）；对照两篇后独有 **1576 字**，其中 120 字是重复 5 次的平台头，**实质 4 段 1456 字**（含一个 30 字章节标题）。这 4 段的 12-gram 主要被 **`新日期拆解`** 吸收（85.0%/95.8%/90.7%/100%），不是 `20260418`（46.0%~68.9%） |
| `防爆头2.0.txt` | duplicate → `20260418` | 与 `防爆头2.0.docx` **抽取后归一化 sha256 相同**；因正本自己被取代，指向**继承**到 `20260418`（否则是断链） |
| `创业科创20cm套利.txt` | duplicate → `科创20cm套利.docx` | **指纹相同**（文件 sha256 不同） |
| `创业板科创板短差的思路.docx` | superseded → `科创20cm套利.docx` | 被覆盖 88%~96%，独有 4~11 段 / 128~313 字 |

**为什么 `20260418` 和 `新日期拆解6.14-19` 两篇都留**：各有 **39% / 43%** 独有内容
（7775 字 / 9572 字），互相替代不了。只留一篇会永久丢掉另一篇近万字。

⚠️ **这个数字我错过两次，都是「分子分母取自不同口径」。** 第一次写「独有仅 9 段/1576 字
（11%）」—— 1576 是对照两篇的结果，却拿单篇总量当分母；正确是对照单篇 16.47%。
第二次改成「实质 3 段 1426 字」—— 把那个 30 字章节标题从**段数**里剔了却没从**字数**里剔，
1576 − 120 = **1456** 不是 1426。教训：写「N 段 M 字」时两个数必须来自同一次筛选。
数字已按实测更正，`quality_report.md` 与 `source.yaml` 的 `notes` 同步改过。

#### ⚠️ 三个必须知道的边界

1. **原始资料是副本，不会自动同步。** 上游在 `C:\Users\20577\Documents\炒股\主升龙头空空龙`，
   KB 里这份是复制进来的（`source.yaml` 的 `upstream_path` 记着）。
   上游更新后要**手动同步再重导**，否则改动不会进库。
   （`register_source.py:49` 硬要求源目录在 KB 根内；实测 Windows Junction 也可行且无此问题，
   但用户选了复制。）

2. **`虚假骗炮封单全维度拆解.docx` 原文残缺。** 末尾停在「虚假封单的峰值量对应流通盘」，
   句子未写完，`document.xml` 总共只有 1157 字符。已建块并标 `risk_flags`，
   但那篇的论述本身是不完整的，不是抽取丢了内容。

3. **10 张行情截图未 OCR，只导出记路径。** 在 `source_libraries/kongkonglong/assets/`，
   清单在 `maps/image_map.jsonl`。实测 OCR 能读出股票名（中际旭创 300308、寒武纪 688256）
   但**数字全是垃圾**（`1367 , 88`、`146 . 98 277 , 76`），CJK 率 0.32 低于
   `chart_ocr_is_useful` 的 0.45 门槛本来就会被拒。图里是「全市场成交额最高的票」这类
   佐证材料，结论在正文里，需要时回看原图。

#### 两个字段在这个来源比别处可靠

- **`claim_type` 有真实区分**：`rule_or_method` 167 / `opinion_or_case` 5。
  这是全库**唯一** `claim_type` 可用于过滤的原始来源 ——
  另六个来源该字段全是导入器写死的常量（见「数据层级与可信度」一节）。
- **`date_precision` 分五档**（2026-08-09 已进索引，可直接用 SQL 过滤）：
  `day`（文件名有完整日期，39 块）/ `day_inferred`（月日可信、年份来自本文正文，47 块）/
  `day_batch`（月日可信、**年份由同批次推断**，14 块）/
  `year_only`（只有年份且为本文明示，9 块）/
  `year_batch`（**连年份都是推的**，63 块）。
  日期敏感的结论要先看这个字段：
  `SELECT ... WHERE date_precision IN ('day','day_inferred')` 才是日期可信的那部分。

#### 踩过的坑（写新导入器时避开）

- **文件 sha256 判不出同源冗余**：两组 docx/txt 同源对的文件 hash 不同、
  抽取后文本 hash 相同。`import_generic_source.py:183` 用文件 hash 判重，
  照抄会让同一句话入库两次。判重必须放在抽取之后。
- **`split_text` 会让 locator 撞车**：一个 2067 字单段被切成 3 块后，三块 locator
  全叫「正文第11段」。既有来源普遍有此缺陷（南京路 214 块、郁金香 158 块、
  波段之门 71 块同文档内 locator 重复），而引用格式用的是 locator 不是 chunk_id，
  所以引用会指向同一位置。本导入器加「之N」后缀避开。
- **共享的 `extract_date` / `clean_title` 在这批文件名上不可用**：
  `extract_date` 对 9 个文件只认出 1 个；`clean_title` 把 `6月20日中午空空龙汇阳财策`
  剥成 `中午空空龙汇阳财策`（日期两边都丢），且对 `20260418-` 前缀无效、会留 emoji。
  已在导入器内自己实现。
- **噪声正则的两个自伤**：① 懒惰量词 + 分支（`[^。\n]{0,80}?(?:A|B)`）在遇到
  第一个锚点就收工，把栏目标题后半截留在正文里（实测 3 块残留）；
  ② 同一噪声写成宽窄两条正则，删完窄的再匹配残留，同一处被计数两次
  （报出的「16 处推广」实际只有 8 处）。都已修。
- **`[^。\n]{0,30}不构成投资建议` 这类无锚点前缀会吃正文** —— 它会连带删掉前面 30 字。
  改成从句子边界或明确起始词（「以上」）开始匹配。

---

## 子项目：_导师试验（导师数字人格提取）

把来源资料提炼成能对话的"数字导师"，产物是 SKILL.md（Agent Skills 协议）。
**与主知识库的分工**：主库回答「作者在哪篇文章里说过什么」（检索），
导师 SKILL 回答「拿这套体系看当前盘面会怎么判断」（推演）。

**⚠️ 两处 SKILL 副本必须手动同步，Claude Code 只加载 `~/.claude/skills/`。**
2026-08-08 实测发现那边是 v1.0 旧版（郁金香 554 行，项目里已是 1172 行 v2.0），
**旧版 `grep "不接受撤单"` 命中 0** —— 用户此前调用时拿到的是缺交易所硬规则、
且未标注 AI 推定阈值的版本。已同步，旧版备份在 `~/.claude/skills/_backup-v1-20260809/`。

**改完项目里的 SKILL 后跑 `python scripts/sync_skills.py --apply`**（先不带 `--apply`
看差异，看清 diff 方向再应用）。这个脚本同时管 `.agents/skills/` 和 `_导师试验/skills/`
两处，并检查 frontmatter 首字段是不是 `name:`。
⚠️ **只拷贝各目录的 SKILL.md 本体，references/ 等辅助文件不跟随**；
HOME 侧若被单独改过（反向漂移），先看预览再决定方向，别盲 apply。

**为什么不用符号链接**：Windows 上 `ln -s` 会退化成目录复制（实测创建后
`[ -L ]` 为假、`ls -la` 显示 `drwx` 而非 `l`），所以无法一劳永逸，只能定期同步。

**⚠️ frontmatter 首字段必须是 `name:` 不是 `skill:`。** 两个导师 SKILL 原来写的是
`skill:`，Claude Code 认不出来，一直没被加载（本机 7 个 skill 里 5 个用 `name`）。
已修正并补了触发场景 description。

**⚠️ 引文可靠性：导师 SKILL 里的引号内容多数无法回溯到语料（各家程度不同）。**
`_导师试验/check_skill_quotes.py` 全量比对：南京路 63 条引文仅 3 条可回溯、
郁金香 34 条仅 2 条 —— 概念是真的（`破局龙` 16 块、`合力票` 75 块），
但句子被改写或生成过；南京路的 `主升段`、`价格破坏`、`空间想象力` 三词在语料里零命中。
其余三家：**波段之门**未机器核查（引文自带出处标注 +「别拿概括当原话」约束）；
**爱在冰川**部分核查（目录内 `verify_mm_quotes.py`，5 个心智模型引文已核实，
法/术层个别引文来自原始 md、无法用 query_kb.py 复核）；
**panfeng-trading** 抽查 4 条仅 2 条可回溯。
各家文首都已加警示。引用前用 `query_kb.py "词" --source <id>` 自己核原文。

| 导师 | 数据来源 | SKILL 版本 | 状态 |
|------|---------|-----------|------|
| 南京路彼岸 | `nanjinglu_bian` 586 块（08-23 实测） | **v4.0.0**（2083 行） | Phase 1-4 完成，30 用例对抗测试（v3.0 后新增部分未复测） |
| 郁金香（潘凤） | `tulip_garden` + `panfeng` 约 1400 块（两源合计） | **v3.1.0** | 30 用例对抗测试 + 12 项修复全落地；T5-T9 已按用户机构口径标定（2026-08-25），T1-T4/T10-T14 待认（v2.0 后新增部分未复测） |
| 波段之门 | `boduanzhimen` 8082 块（08-23 实测） | v1.0.0（429 行，单文件） | 建成，**未做对抗测试** |
| 爱在冰川 | `aizaibingchuan` 7338 块（08-23 实测） | v1.0.0（538 行） | 建成，**未做对抗测试** |

⚠️ 南京路 v4.0.0 / 郁金香 v3.0.0 均出自 commit `4b5067a`（2026-08-09「先查库再回答」改造），
此前 README 漏记了这次升级，2026-08-23 审查补记。

另有 **panfeng-trading**（2026-08-23 从 `~/.claude/skills/` 收编进 `_导师试验/skills/`）：
不是导师人格，是潘凤语料落到当日决策动作的执行层（九步决策链，
输出【结论】出手/不出手 + 【仓位】上限%）。它与主项目「不做的事」里
「不生成荐股结论」只隔一步，靠三道约束兜着（结论挂语料出处和日期 /
语料边界自述仅约六周 / AI 不代执行、不连券商），详见 `_导师试验\README.md` 收编记录。
其运行时状态 `state/current-state.md` 故意只留在 HOME，不进项目正本。

**郁金香对抗测试 + 修复（2026-08-05 同日）**：30 用例测出 🔴6 / 🟡7 / ⚪4，
比南京路 v1.0（3/5/4）多；按 12 项建议全部修完，554 → 1157 行，v1.0.0 → v2.0.0。
两条最重的及处置：① 卡位战法的 9:20-9:25 窗口**交易所不接受撤单**，原文零提及
（[上交所交易规则](http://www.sse.com.cn/lawandrules/sselawsrules2025/repeal/rules/c/c_20180806_10784917.shtml)）——
v2.0 在模型3 补了时段表、三条推论和仓位上限（倒推口径「假设开盘一字跌停且当天卖不出」）；
② **术语污染**，模型4 在用南京路的「天道」，而「天道」在郁金香 1367 块里**零命中** ——
v2.0 模型4 改名「情绪流循环原则」（「情绪流」郁金香 71 块 / 南京路 0），
边界条件的「天道压制」换成「监管异动」（郁金香 11 块）。
报告 `_导师试验\skills\yujinxiang-perspective\adversarial-test-report.md`（1817 行）保持原样。

**⚠️ v2.0 新增的 14 条量化阈值（T1-T14）是 AI 推的，不是郁金香原文。**
郁金香原文一个数字都没给，这批数字按 A 股常识和南京路对应部分补的，
每条标 `⚠️T<编号>` 并集中在 SKILL 文首「参考阈值总表」一节，改一处全文生效。
**用户认之前，引用这些数字必须说明它们不是导师原话。**
**2026-08-25 更新：T5-T9 五条已由用户按机构量化口径标定**（流通盘三档分层 /
台阶 10-25% + 级间整理判据 / 断层日均+累计双条件随涨停制度缩放 /
填实 20%+量时组合 / 首板按市值分档），SKILL 升 v3.1.0——已获认可但**仍非导师原话**，
引用时说明「用户标定的执行参数」；T1-T4、T10-T14 仍是 AI 推定待认。

**隔离原则**：只读 `_知识库系统/indexes/knowledge.db`，不写主项目任何文件，
整个 `_导师试验\` 可以直接删掉不影响主库。

**几个导师 SKILL 会互相冲突，这是设计使然。** 南京路启发式 1 是「不追高，等回调或次日」，
郁金香卡位战法是「9:20-9:25 提前挂单」—— 前者等，后者抢。四家时间尺度各不相同
（南京路周期级 / 郁金香日内级 / 波段之门波段级 / 爱在冰川隔日超短），
同时用几个 SKILL 时要清楚在用哪套。

**当前所有分数都是 AI 预估，没有一个是实测值。** Phase 5 需要人工用题库跑分
（南京路 23 题、郁金香 30 题），AI 自评没有意义。
**2026-08-25 用户决定：人工评分与剩余阈值确认整体搁置**——自述还没实际用过导师、
没空测试、「之前用的感觉都很糟糕」。这些不再作为活跃待办；导师质量改由
日常真实使用反馈驱动（体感差就说具体哪里差，按单点修，不跑成套题库）。
⚠️ 若用户的糟糕体验发生在 2026-08-09 之前，很可能用的是 HOME 侧 v1.0 旧副本
（缺交易所硬规则和全部修复，当日已换新）——重试前先确认用的是新版。

爱在冰川已于 2026-08-09 建成第三个导师（v1.0.0），波段之门同日建成第四个（v1.0.0）。
两家都**未做对抗测试**，是四家导师当前最大的质量缺口（README 待办第 5 项）。
当时的可行性分析（图表未 OCR、持仓打码、`date` 是发布日）保留在 README 作过程记录，
其中两条后来被实测推翻：图文关联（`image_path`）已大幅缓解图表问题；
「持仓打码」判断错了 —— 他自述「贴标的、贴盈亏，**不贴仓位**」，决策链是完整的。

详细进度、文件结构、剩余待办以 `_导师试验\README.md` 为唯一事实来源。

---

## OCR 引擎（2026-08-24 切换：RapidOCR 为主，dual 双引擎保底）

**入口在 `kb_import_utils.ocr_images(engine=...)`**，预处理（EXIF 转正/缩宽/竖向分片）两引擎共用一套。
默认 `rapid`（rapidocr_onnxruntime，模型随包自带、纯 CPU、不联网）；环境变量 `KB_OCR_ENGINE=windows` 可整体回退。

| 层 | 引擎 | 坏块率变化 | 说明 |
|---|---|---|---|
| 波段之门 chart_ocr | rapid 纯跑 | 14% → **0%** | 汉字量持平（49123→49192），块更少更干净 |
| 爱在冰川 course_ocr | rapid 纯跑 | 92% → **0%** | 汉字量持平；数字仍需回看原页 |
| 南京路 screenshot/article(ocr) | **dual** | 29%/35% → **14%/17%** | 汉字量 -116（基本持平）；受灾片段保留率 100%/86% |
| 郁金香 image_ocr | **dual** | 25% → **21%** | 汉字量 **+3137** 反超旧版；四篇受灾文档保留率全部 100% |
| 波段之门 paid_article | **dual** | 26% → **20%** | 《瑞》文可信片段保留率 4% → 100% |

ground truth 基准：南京路文本层页还原率 Windows 40.2% vs Rapid **92.4%**（`_bench_ocr.py`）。

坏块率现值以 `reports/ocr_quality_report.md` 为准（`python _知识库系统/scripts/report_ocr_quality.py`
重新生成；2026-09-05 实测：chart 0% / course 0% / nj 截图 11.4% / tulip 18.5% / paid 17.3%）。

**为什么南京路/郁金香/付费层必须 dual**：全量对比发现 rapid 有两种抽样测不出的失败——
①**半读**：《题材是否抬头》只认出页面一角（453/4198 字），18 个可信片段 0% 存留；
②**假汉字乱码**：郁金香美图长截图输出「写K不_TT」式乱码，字数虚高。失灵判据
（汉字过少 / 拉丁占比>0.40，`kb_import_utils._rapid_degenerate`）拦不住半读，
所以这三处两台引擎都跑、逐图取汉字多者。代价是每图两次识别，仅给小体量来源用。

缓存版本键现况：nanjinglu `CACHE_VERSION=8`、tulip `OCR_CACHE_VERSION=5`、
boduanzhimen 图表 `OCR_CACHE_VERSION=2` + 付费页独立 `PDF_OCR_CACHE_VERSION=1`
（此前波段之门两类缓存都没有版本键，换引擎不会触发重算——已补齐）。改引擎/预处理必须 bump 对应键。

被拒 K 线图（~3100 张）门槛放宽评估结论：**不放宽**。缓存原始文本粗探显示放宽到
16 字/0.30 也只能救回 28%，且救回的多是行情面板残渣与数字汤。新引擎下若个别图
质量跃升，属缓存自然更新，无需动门槛。

## 数据层级与可信度

```
原始文件（只读）→ Parent（上下文单元）→ Chunk（检索定位）→ 方法卡（核心产出）
```

| 层级 | 可信度说明 |
|------|-----------|
| 原始文件 | 最高，需人工阅读 |
| Parent | 高，回答的主要依据 |
| Chunk | 仅用于检索定位，不能只看摘要 |
| 方法卡 status=reviewed | 高，用户已确认 |
| 方法卡 status=draft | 草稿，待确认 |
| claim_type 字段 | **六个来源不可信**（均为写死的固定值，绝大多数是 `opinion_or_case`）；**唯一例外是 `kongkonglong`**：`rule_or_method` 158 / `opinion_or_case` 5，按文档性质实标，可用于过滤 |
| confidence 字段 | 只有南京路、郁金香可信；`aizaibingchuan` 正文部分与 `panfeng` 固定 `high`，`fulibei` 固定 `medium` |

**`confidence=high` 不等于高可信。** 2026-08-24 重测取值分布（**此前文档的结论是颠倒的，
已改正——四源有真实梯度，唯一写死的是 kongkonglong**）：

| source_id | high | medium | low | 其他 | 是否有真实区分 |
|-----------|------|--------|-----|------|--------------|
| `aizaibingchuan` | 7289 | 36 | 13 | — | 正文写死 high；medium/low 全在额外资料与讲义 |
| `boduanzhimen` | 6939 | 1143 | — | — | 有 |
| `tulip_garden` | 511 | 702 | — | — | 有 |
| `nanjinglu_bian` | 252 | 104 | 228 | — | 有 |
| `panfeng` | 186 | — | — | — | 无，写死 |
| `fulibei` | — | 1444 | — | curated 26 | transcript 写死 medium |
| `kongkonglong` | 172 | — | — | — | **无，写死**（但该来源的 `claim_type`/`date_precision` 有真实区分） |

跨来源按 `confidence` 排序仍会把 aizaibingchuan/boduanzhimen 的高分块整体顶前，
这是假象；但旧版「六源全写死、仅南京路/郁金香可信」的说法不成立。

**南京路检索优先级：** `chunk_type=article` + `confidence=high`（文本层正文）
优先于 `medium`（OCR 正文）优先于 `screenshot_ocr`（截图，须标注低置信度）。

**chunks 表含三类内容：** 索引里的 `chunks` 表不只是原始切块，还包含方法卡（`methods.jsonl`）和分歧卡（`conflicts.jsonl`）写入的条目。`build_index.py` 统计输出的"块数"含这三类，不等于原始切块数。（来自报告问题 2.3）

### chunks 表现在有 20 列（2026-08-09 加了 2 列）

新增 `extraction_method` 和 `date_precision`。此前两者只在 `chunks.jsonl` 里，
检索侧取不到 —— 而文档写着「日期敏感的结论要先看 `date_precision`」，那句话在
`query_kb.py` 里根本做不到。现在能用 SQL 直接过滤了。

| 列 | 有值块数 | 覆盖来源 | 用途 |
|---|---:|---|---|
| `extraction_method` | **17450** / 18920 | 6 个（仅 `fulibei` 缺） | 区分「文本层抽的」与「OCR 认的」。取值 `ocr` / `pdf_ocr` / `ocr_screenshot` / `pdftoppm+windows_ocr` 的块，**里面的数字不可信**，引用前回看原页 |
| `date_precision` | 172 | 仅 `kongkonglong` | 日期可信度五档，见该来源专节 |

**空值是合法的**，表示该来源的导入器没有评估这个维度，**不等于「可信」**。
拿 `date_precision=''` 筛不出「日期准的块」，只能筛出「没评估过的块」。

⚠️ **`build_index.py` 的列顺序现在由 `CHUNK_COLUMNS` 常量单一定义**，建表语句和插入
语句都从它推导，FTS 也改成按列名取值。改动原因：原先是「建表 SQL 写一遍列名 +
插入写一个 18 元素元组 + FTS 按 `row[6]/row[8]/row[14]` 取下标」三处各自维护，
靠一条注释提醒「新列一律追加末尾，否则下标错位」。那是靠人记的约束 ——
手滑插在中间，FTS 就会把 `locator` 当 `text` 索引，**而且不报错**。
现在加列不必再关心位置。

**方法卡 20 张已于 2026-08-08 全部审批通过（`status: reviewed`），已进索引。**
审批走 `reports\方法卡审批清单.md`：用户逐张填 `认/改/拒`，再由
`scripts\apply_method_review.py` 解析回写 `methods.jsonl`（`--dry-run` 可预览）。
结果：20 张全部 `认`，其中 **16 张附带用户未解问题**。

**`user_questions` 字段是这套卡的关键补充。** 用户的疑惑高度集中在一件事上：
卡回答的是「该看什么」（What），而用户问的全是「怎么看出来」（How）——
8 张问「怎么判断」、5 张问「怎么执行」、3 张说「太虚，不知道怎么做」。
这不是卡的缺陷，是方法卡的固有边界：原文本身就没给可执行步骤
（如 method-010 华东大导弹只说「卖点和买入模式有关」）。

**所以绝不能由 AI 编操作细则去填这些缺口** —— 项目里已有前例：郁金香 SKILL v2.0
那 14 条阈值是 AI 推的，郁金香原文一个数字都没给。正确做法是把缺口显式记进
`user_questions`，随卡一起进检索正文（前缀「用户未解问题：」），
检索到卡时一并显示，避免把半完备的卡当成可照做的规则。

这 16 条疑惑同时是**最好的评测题**——比人工编的 benchmark 更贴近真实使用。

**已用 `scripts\probe_user_questions.py` 探库，诊断结论：「讲过但搜不到」，不是「没人讲过」。**
证据是同一概念的书面语与作者口语召回量级差极大（探针 → 全库召回）：

| 用户/书面说法 | 召回 | 作者实际用词 | 召回 |
|---|---:|---|---:|
| 情绪核心 | 2 | 龙头 / 大票 | 1825 / 312 |
| 容量核心 | 8 | 权重 / 高位股 | 397 / 125 |
| 买卖计划 | 1 | 推演 / 卖点 / 预案 | 256 / 269 / 78 |
| 板块阶段 | 1 | — | — |
| 边际强化 | 1 | — | — |
| 模式验证 | **0** | — | — |

13 个低召回探针几乎全是书面术语，作者们的口语说法召回都在几百上千条。
**所以缺的是「把用户的问法对齐到作者的用词」这一层，不是缺内容。**
修法优先级：① 扩同义词表（最便宜）② 语义检索（根治）。加来源解决不了这个问题。

结果表 `reports\未解问题探库结果.md`。⚠️ 该脚本的概念词是**人工登记**的
（`CARD_CONCEPTS`），不自动抽词——试过「删停用词+正则切中文」，产出的是
「谁是情绪核」「有没有增量」这类断裂碎片，召回恒为 1（命中的正是卡里那条疑惑
本身），是纯噪声。中文没有空格分词，靠删词切不出概念。

⚠️ **方法卡审批状态会改变一批测试基线**，两个方向都不是缺陷：
draft 时 fulibei 1450 / `情绪周期` 505 / `弱转强` 110 / `打板` 183；
reviewed 后回到 SPEC 0.1 节冻结值 1470 / 516 / 111 / 186。
实测方法卡对各词的命中贡献：`竞价` 1、`龙头` 1、`打板` 3、`情绪` 4、
`情绪周期` 2、`弱转强` 1。将来若有卡改成 `revised`/`rejected`，这些数字会再降，
届时按「reviewed 卡数」重新对账，别把实测值直接抄进测试。

### 回归基线怎么改（不要直接编辑测试代码）

基线已外置到 `indexes/regression_baseline.json`，`test_query_kb.py` 读它做断言。
样本是 `fulibei` / `nanjinglu_bian` / `tulip_garden` 三个来源（**不含**后接入的
`aizaibingchuan` / `boduanzhimen` / `panfeng` / `kongkonglong`）。

```bash
python _知识库系统/scripts/refresh_regression_baseline.py          # 对账，列差异
python _知识库系统/scripts/refresh_regression_baseline.py --write   # 确认合法后写入
```

**写入后必须往 `history` 追加一条说明 why** —— 脚本不会替你写，
没有 why 的更新等于取消这项保护。

⚠️ **「命中块数下降」不等于内容丢失。** 2026-08-09 南京路整源重跑双路抽取后，
`情绪` 896→877、`龙头` 630→613、`情绪周期` 519→517 都降了，但该来源总字符
228602→284193（**+24%**）、`screenshot_ocr` 186→227 块（字符 +70%）。
原因是切块边界变了、高频词在块级别的分布随之改变 ——
词的总出现次数没少（`情绪` 482 次、`龙头` 488 次）。
对账时要把这两件事分开，否则会把正常重跑误判为污染。

⚠️ **JSON 的 `_更新规则` 里曾写 `--check` 参数，那个参数不存在**（会报
`unrecognized arguments`），不带参数就是对账模式。已改正。

---

## 常用同义词（检索时扩展）

括号内是**实测命中块数**（2026-08-04 增量导入后对 knowledge.db 重测，`text` 列 GLOB 子串匹配）。
⚠️ **下面这张表的括号数字是 2026-08-04 在 9175 块上测的，已过时。**
当前索引 **18901 块 / 7 个来源**（见本文件开头的来源表）。
三次变化：爱在冰川 5813→7338（修 MIN_AUTHOR_REPLY）、新增波段之门 7169→8187、
南京路彼岸 525→551（新增《风起云涌》）。
**要用准确数字请跑 `python scripts/check_synonyms.py`**，它按当前库重测每个词。

数字小的词照样要查 —— 低频不等于无效，`先于龙` 只有 9 块但 7 块在南京路，是该来源的独特说法。

**这张表是给人看的，机器版在 `config\synonyms.yaml`（2026-08-24 已升 v6：41 组 / 208+ 词；
新增十字路口→节点族、高位分歧、板块阶段、边际强化、无机会五组用户问法对齐，
补齐主干/滑点/大小题材三处「注释认领了但词没进 variants」的半成品）**，
由 `query_kb.py --expand` 读取。配置文件是唯一被代码消费的事实来源，
且它的命中数按 18875 块重测过（现库 18901，命中数会略偏低）。v4 相对本表多了四组波段之门的词汇
（波段 / 浪型 / 背驰 / 指标公式）。

**`check_synonyms.py` 现在会区分「作者用词」和「读者用词」**：
某词只出现在 `问：` 段说明是读者说的，收进同义词表只会捞到读者的话。
实例：`由弱转强` 全库 1 块且在读者提问里，作者本人只说 `弱转强` —— 故留在死词区。

### 整句提问可以直接用了（2026-08-08 修）

以前整句输入会返回「未找到匹配结果」：`terms_from_query` 只按空格和标点切
（`re.split`），中文长句切不开，「怎么判断情绪周期拐点」被当成一个 **10 字词**
去做 FTS 精确匹配，库里当然没有这个连续字符串。实测：

| 输入 | 修复前 | 拆成词 |
|---|---:|---:|
| `怎么判断情绪周期拐点` | **0 条** | `情绪周期` 8 条 / `拐点` 8 条 |

**这条最坑的地方是它像「库里没有」而不像 bug** —— 用户自然会整句提问，
拿到 0 条就以为作者没讲过。现在 `search()` 返回空时会按已知术语切词自动重试，
并打印「整句无结果，已按术语切词重试：情绪周期 / 拐点」让你看得见发生了什么。

术语表从 `synonyms.yaml` 汇总（141 词），按长度降序匹配 —— 先长后短，
`情绪周期` 不会被先切成 `情绪` 再剩 `周期`。**只做术语提取不做通用分词**：
通用分词要引依赖，且会把「怎么」「判断」这类疑问词也切出来，反而降精度。

### 检索与回答质量的四项速赢（2026-08-24）

demo1 十二题重跑暴露的四个问题同日修完（`query_kb.py`）：

1. **预览截断 420→1200 字**（`--preview N` 可调）：方法卡曾被打成半句
   （「知道适」「怎么判断大小题」），一道半答可能就是截断造成的。
2. **同形词降噪闸门**：多词查询里「只命中短拉丁/数字弱词」的块整体后置——
   「plan abc」曾把「ABC 板块博弈」「abc 浪形」捞进 Top4。
3. **兜底重切扩到四个触发条件**：零结果 / 全无正文命中 / 只命中字母串 /
   强候选≤1 但输出被弱块塞满。整句里的中文粘成长伪词时（如「怎么做次日的买卖计划」）
   原本只能靠 plan/abc 字母串命中，现在自动切词重试。
4. **`--expand` 默认开**（`--no-expand` 反向）；查波段之门建议 `--no-expand`
   （同义词表是短线体系建的，对他家引入噪声，见 `reports/boduanzhimen_改进清单.md` P1-1）。

重跑结果：`reports/_demo1_raw_v2_20260824.txt`——带噪声案例清零、截断复原，
其余不劣于基线。注意基线口径的已知水分：八道能答题的首位命中全是方法卡，
卡文里嵌着与提问近乎逐字的「用户未解问题」，检索部分是在匹配卡片自己记录的缺口。

| 概念 | 各来源常见写法 |
|------|--------------|
| 情绪周期（319） | 冰点（485）、高潮（491）、分歧（573）、退潮（269）、情绪冰点（65） |
| 弱转强（263） | 转势（54）、强转弱（6）、走弱转强（1，复利杯独有） |
| 龙头（1511） | 龙一（45）、龙二（45）、核心票（27）、先于龙（9，主要在南京路）、人气核心（2） |
| 主力（190） | 机构（1086）、散户（405）、游资（298）、大资金（259）、北向资金（267，冰川独有）、外资（195）、庄家（31） |
| 入场（169） | 打板（1350）、低吸（1228）、潜伏（573）、开仓（455）、追高（325）、半路（211）、埋伏（154）、进场（136）、起步（50） |
| 竞价（1341） | 高开（778）、低开（651）、集合竞价（217）、竟价（129，错别字，**128 在郁金香**）、竞价抢筹（46） |
| 连板（946） | 反包（768）、首板（719）、二板（361）、三板（256）、烂板（212）、断板（180）、板高（43）、连板高度（15） |
| 涨停归因 | 涨停动因（1354，**冰川独有**）、动因（1365）、涨停原因（9） |
| 卖出与下跌 | 割肉（842）、杀跌（834）、利空（560）、止损（335）、跳水（258）、砸盘（161）、补跌（113） |
| 仓位（986） | 空仓（454）、加仓（363）、满仓（298）、清仓（205）、减仓（201）、半仓（130）、底仓（56） |
| 缩量放量 | 缩量（1129）、放量（793）、均线（283）、影线（247）、上影线（136）、十字星（91）、长上影（20，冰川独有） |
| 高送转 | 送转（528）、高送转（427）、填权（178，冰川独有）、业绩报（151，冰川独有）、除权（92）、预增（76） |
| 自选池 | 票池（297）、自选股（49）、股池（5，冰川独有）、观察池（2，郁金香独有） |
| 监管异动 | 异动（381）、监管（360）、特停（124，122 在冰川）、卡异动（3）、被关小黑屋（2，复利杯独有） |

**「入场」上一版写「无此词」是错的，实测 169 块。** 旧版大概是把它当纯概念名没测，
这次逐词重跑（`azbc_recount3.py`）确认冰川 157 / 复利 6 / 郁金 4 / 南京 2。

**死条目会被新来源复活。** 上一版标注「全库 0 命中」而删掉的 `极度低迷`、`空间龙`，
爱在冰川接入后分别有 3 块和 5 块（全在冰川）；`无人关注` 这次也复活了 1 块（在冰川）。
仍为 0 的只剩：`市场最悲观`、`由弱转强`、`先弱后强`、`入场时机`。
每次接入新来源或做增量导入都要重测一遍，不能沿用旧判断。

**同义词不能反向替换。** `弱转强` 在复利杯有 43 块，但 `转势` 在复利杯**零命中** ——
扩展只能加词，不能拿同义词顶替原词。同理 `北向资金`、`涨停动因`、`填权` 只在爱在冰川
出现，拿它们检索等于只查一个来源。

**高送转类词现在会捞到课程讲义 OCR。** `高送转` 427 块里有 10 块、`送转` 528 块里有
11 块、`除权` 92 块里有 4 块来自 `chunk_type=course_ocr`（`confidence=low`，
正文与 K 线乱码混排）。这批是全库唯一系统讲高送转打法的素材，值得看，
但**里面的价格、日期、涨幅数字不可信**，引用前必须回看原页。
另有第三方摘录 `curated_digest` 也会被捞到（`龙头` 19 块、`低吸` 17 块、
`送转` 3 块、`高送转` 2 块、`填权` 1 块），那不是作者原文，按 `author` 区分。

**载体特征词不是交易术语。** `借楼`（963 块）是爱在冰川留言区回复的固定开头
（「答：借楼一用：…」），`各种数据`（1420）、`方向展望`（385）、`瞎扯淡`（181）
是该来源的固定小节名。用它们检索筛出的是结构位置，不是观点。

**同一个词在不同来源可能指不同概念**，归一前先看该来源自己的定义。
实例：「容量」在南京路指市场资金容量，在郁金香指个股换手承接能力。

---

## AI 工作规则

**先检索，再回答。** 不用模型记忆代替检索结果。

**交易概念/观点/判断类问题（"X 是什么"、"某人怎么看 X"、"这种情况怎么判断"、
"某说法对不对"）一律按 `/kb-ask` 的流程走：`query_kb.py` 查库 → 读命中块 →
先答案后出处地回答。库里没有就说库里没有，禁止拿模型常识冒充作者观点。**
这条是 2026-08-23 方向校准的核心规则：本项目第一定位是一问一答的问答库，
任何会话里用户问交易问题，默认行为就是查这个库，不需要用户点名"去库里查"。

**说清用的是哪层：**
- 来自方法卡：说明 status 是否 reviewed
- 来自 Chunk/Parent：给出来源定位
- 来自模型常识：明确说"这是一般性说明，不来自库内资料"

**引用格式：**
```
[来源 | 作者/嘉宾 | 文档 | 日期 | 页码/时间戳]
```
OCR 不确定时标注 `⚠️ OCR低置信度`，说话人不确定时标注 `⚠️ 说话人待核实`。

**历史观点 ≠ 当前市场事实。**
问题含"今天、现在、目前、是否仍有效"时：库内资料只说"作者历史上怎么说"；当前事实通过 Web/MCP 核验；两者分开写，注明截止日期。

**行情数据统一走 `live-market` MCP**（自建，**73 个工具**，短线 + 盘中 + 盘后全覆盖）。
脚本 `live_market.py`（实时基础）+ `live_market_ex.py`（股池/历史分钟/资金流/盘后）
+ `live_market_ths.py`（短线专用：涨停归因/连板天梯/题材聚集度）
+ `live_market_kaipanla.py`（开盘啦：情绪历史序列/大单分时/百日新高）
+ `rps_local.py`（RPS 三线红本机直算）+ `rps_pool.py`（名单层，7 指标第 7 项）。
东财 push2delay / push2ex / datacenter-web + 同花顺 data.10jqka + 腾讯 + 新浪
+ 开盘啦 apphis.longhuvip.com，多源自动降级，免费无需注册，延迟 1-3 秒。

73 的构成（2026-08-05 从 `mcp.list_tools()` 实数）：`kpl_` 前缀 28
+ `rps_local_*` 3 + `rps_pool_*` 3 + 东财/同花顺/聚合 39。
**开盘啦不当主源**——它当日多个端点静默返回 0，
详见 `scripts\开盘啦集成说明.md` 的「按维度选源」。

**7 指标第 7 项不联网，直读本机通达信。** 三线红要全市场 50/120/250 日涨幅百分位排名
（RPS），网络源全都不给；但通达信已经把 RPS 算好存在 `C:\new_tdx\T0002\extdata\` 了，
`rps_local.py` 直读磁盘算，不用写选股公式、不用手工导出。你每天只需在通达信里
下载盘后数据，然后 `rps_local_calc(save=True)`。本机有 4235 个可算交易日，
`rps_local_backfill` 一条命令就有历史。**板块三线红（BKRPS 编号 7–11）本机没装，
不算也不假装能算**。口径、验证记录、另一条导出路见 `data\RPS数据获取配置.md`。

调用顺序：
1. **短线看盘先 `get_shortline_board`**（题材主线 + 连板天梯 + 归因热词，约 2KB）
2. `get_market_temperature` 定位情绪周期
3. 封板结构看 `get_zt_pool_detail`（带首封时间、炸板次数、封单额、封成比）
4. 「为什么涨停」看 `get_stock_reason`（同花顺归因全文，行业原因 + 公司原因）
5. 弱转强/反包看 `get_yesterday_zt`（昨涨停今日表现 + 红盘率）
6. 冲高回落看 `get_intraday_shape`（量化成数字，可回溯约 60 个交易日）
7. 盘后复盘看 `get_after_close`（一次取齐 + 自动算封板成功率、炸板率）

**两套涨停数据分工，别重复调：** 东财 `get_zt_pool_detail` 给钱的维度（封单额、封成比），
同花顺 `get_zt_reason` 给逻辑的维度（涨停归因标签、官方封板成功率）。
**「连板数」两家口径不同**：同花顺「3天2板」是最近 3 日涨停 2 次（不要求连续），
东财「连板数」是末尾连续涨停天数。都对，问的不是同一个问题，别互相印证。

完整接口对照表、判读口径、数据发布时点、已探测确认不可用的源见 `/market-data-toolbox`。
第三方 `stock_data` MCP 只用筹码 `stock_chip`、历史日频资金流 `stock_fund_flow`、
所属板块 `stock_sector_spot`、快讯 `stock_news_global` 这几个；实时行情类已熔断，
`akshare_one` 整体不可用，别调。

**`akshare` 本机可用**（1.17.80 已装），但必须在 `import akshare` **之前** pop 掉
proxy 环境变量并设 `NO_PROXY="*"` —— `trust_env=False` 管不到库内部自建的 session。
上一版记「akshare 全挂」是误判。它能补的是我现在没有的：营业部排行（带上榜后胜率）、
业绩预告、高送转、大单追踪、停复牌。**没接进 MCP**（同步阻塞、部分接口本机不通），
要用就在脚本里临时 import。

**自己归纳字段含义之前先翻现成库的源码。** 2026-08-04 实证：盘口异动 22 个 type 码，
我在东财页面和 13 个 JS 里两轮 0 命中后按实测特征归纳，结果 **15 个名字是错的**；
而 akshare 源码 `stock_pankou_em.py` 里就有官方 `symbol_map`。已改用官方映射。

**盘后数据有发布时点，查不到先看时点再怀疑接口。** 龙虎榜和大宗交易盘后才出，
盘中查当天返回 `数据状态: 尚未发布` 是正常的；两融是 T+1。
取数失败就说失败，不用模型记忆里的行情假装是实时数据，也不拿前一天的数据冒充当天。

**来源之间有分歧时并列呈现，不合并。找不到证据就说找不到。**

---

## 待修复事项

| 优先级 | 问题 | 影响 | 方案 |
|--------|------|------|------|
| ~~P0~~ | ~~南京路：有文本层的页面被错误 OCR~~ | 已修复 2026-08-02 | 双路保留，见上方小节 |
| ~~P0~~ | ~~郁金香：切块过碎~~ | 已修复 2026-08-02 | 见上方郁金香小节 |
| ~~P0~~ | ~~FTS trigram 无法匹配两字词~~ | 已修（阶段 2，2026-08-04 在 9175 块上复测） | 短词分流走 GLOB，分词器没换。实测 `龙头` 1825、`板块` 2958、`竞价` 1399、`打板` 1358、`低吸` 1248、`卡位` 209、`筹码` 772、`反包` 771，八个两字词的 `search()` 返回值**逐个等于三字段 GLOB 全量**。裸 `MATCH` 仍是 0，那是 trigram 的固有属性，不影响检索 |
| ~~P0~~ | ~~LIKE 兜底候选池按 rowid 截断~~ | 已修（阶段 2） | 兜底连同 `candidate_limit` 一起删了，改为取全量候选再在 Python 侧排序（`query_kb.py:607` `rank_and_truncate`）。**此前本表写的「前 50 条 100% 全是爱在冰川」是错的** —— 那个数字来自手写 `ORDER BY rowid LIMIT 50` 模拟，而那条代码路径已不存在。走真实 `search()` 实测：`龙头` 前 50 是复利杯 28 + 冰川 12 + 南京路 10，`筹码` 前 8 是郁金香 8/8 |
| P1 | 短词检索耗时随语料线性上涨 | 9175 块下 `板块` 150ms、`龙头 竞价 弱转强` 246ms。短词模式凑不出 trigram，必然全表扫描，语料再翻倍就要上秒 | 2026-08-04 已做一轮：三列 UNION 合成 `chunks` 表单次 OR 扫描，1.4–1.7 倍（见 `query_kb.py` 的 `SHORT_TERM_RECALL_SQL`）。下一步只能换召回结构（如两字词倒排表），不是调参能解决的 |
| ~~P0~~ | ~~`build_index.py` 不按 `sources.yaml` 的 `status` 过滤~~ | 已修复 2026-08-04 | 读 `sources.yaml` 白名单（`integrated` / `integrated_first_pass`）；新增来源落盘但 `status` 不在白名单，索引自动跳过并记入 `skipped_sources`。`source_name` 硬编码"复利杯"同步修复，现从登记表取 `display_name`。方法卡加 `status` 字段门槛：只有 `status=reviewed` 的卡进索引，全部 20 张当前为 `draft`，需人工逐一审批后改状态。`validate_kb.py` 同步加入 status 一致性和索引来源合法性两项校验 |
| ~~P0~~ | ~~导入脚本用 `yaml.safe_dump` 重写整个 `sources.yaml`，把注释和手写格式全抹掉~~ | **已全部修复 2026-08-24**。`import_kongkonglong.py`（08-09）、`register_source.py` 与 `import_generic_source.py`（08-24，改为打印待手改的登记块/字段）；经查 `import_nanjinglu.py` / `import_aizaibingchuan.py` 本来就不碰登记表（旧文档说法有误）。代价是登记表靠人手改——`validate_kb.py` 的「登记表块数一致性」检查兜底（本轮它就抓到 nanjinglu 登记 586、实际 584），重导后记得手改 `sources.yaml` 的 `last_import_summary` 并核对注释还在 |
| P1 | 各来源 claim_type 均为固定值 | 元数据过滤失效 | 规则+LLM 重新标注。**例外：`kongkonglong` 已有真实区分**（`rule_or_method` 167 / `opinion_or_case` 5）；fulibei 的策展块也有（方法卡 rule 20 / 分歧卡 opinion 6），但 transcript 主体仍是常量 |
| ~~P1~~ | ~~复利杯闲聊未标记~~ | **已修 2026-08-24** | `mark_fulibei_chitchat.py`：规则候选 25 条、人工逐条核对后标 22 条 `chunk_type=chitchat`，排序层 -3 降权；实测「龙头」top1 从寒暄块回到战法内容。已标记的块仍可检索 |
| ~~P1~~ | ~~`add_chunk` 的 NOT NULL 列被空串绕过，`chunk_type` 缺失时默认 `"text"`~~ | 已修 2026-08-09 | SQLite 的 NOT NULL 只挡 None、不挡 `""`，而 `item.get(name, "")` 恰好把「字段缺失」变成空串 —— 没有正文/定位/标题的块能静默入库，检索时是查得到却读不懂的空壳。`chunk_type` 更隐蔽：兜底值 `"text"` 不在任何来源的 21 个真实取值里，按类型过滤会永远漏掉这批块。现在 `add_chunk` 显式拦截，并加 `KNOWN_CHUNK_TYPES` 白名单（新增类型必须登记，否则建索引失败）。测试 `ChunkFieldGuardTests` 8 个用例锁住，含一条反向断言：真库里出现的 `chunk_type` 必须都已登记。**当前数据本来就干净（4 列零空值、无 `text` 类型），这是防将来的加固** |
| ~~P1~~ | ~~判「举例年份」用引导词黑名单，双向都不准~~ | 已修 2026-08-09 | 旧实现 `(?:比如\|例如\|回顾\|早在…)[^。！？\n]{0,12}?(20\d{2})年` 实测：30 个举例句式**漏 19 个**（`像2021年那波`、`拿2019年来说`、`以2017年为例`、`2015年那波杠杆牛` 都不含表里的词），10 个作者自指句**误排 6 个**（`例如2025年初我就说过` 同时含引导词和自指）。讽刺的是那版注释里举的「该保留」例句 `早在2026年初就锚定了国产替代` 自己就在黑名单里。**引导词是无穷集合，黑名单注定漏；自指句也用引导词，注定误伤。** 换判据：不猜句式，只判「这个年份配不配当成文时间」—— 成文只可能是当年或前一年，更早的一定在讲历史。新判据举例漏 **0/11**、自指误排 **0/7**，混合句能只留近期年份，无参照年时返回空（宁缺勿错）。真实 14 篇日期一字未变。测试 `WritingYearTests` |
| P1 | 波段之门连发留言曾被错配同一条占位提问（如「Feng Jian:沙发」）—— **主问题已修**：重导后连发的第 2 条起改用独立前缀 `作者续言（承接本篇第N条，同一串第M条）：…`（实测 641 块），不再冒充在回答上面的读者留言；引用续言别拿上面那条提问当语境。**未核**：原方案里的候选 question 占位过滤（命中就按 `作者留言：xxx` 处理）是否单独实现过 |
| P1 | 复利杯闲聊未标记 | 检索结果夹杂无关内容 | 识别并标记 chunk_type=chitchat |
| ~~P1~~ | ~~爱在冰川 2023 三季度时间空洞~~ | 已补齐 2026-08-04 | 用户补下载 `.md`，增量导入 71 篇（+131 块）。补完后该季度 65 个工作日只剩 7 天无内容，属作者未发文，见上方小节 |
| ~~P1~~ | ~~爱在冰川 127 篇整篇未入库~~ | **用户 2026-08-04 决定不救** | `MIN_DOC_CHARS` 保持 100。这 127 篇内容永久不在库内，其中 30 篇作者实写 ≥60 字，需要时回看原文。清单留档在 `06_待你确认_127篇清单.md` |
| P2 | 干货点摘录与库内原文 3-8 成重复 | 同一观点可能检索到两次（一次原文、一次摘录），计数会虚高 | 已用 `author` 字段区分；如需去重再做 n-gram 比对 |
| P2 | ~~南京路 jpg/pdf 双源并存~~ **已修（2026-09-05）**：排序第三层给 `extraction_method=embedded` 加 +0.25（`EXTRACTION_EMBEDDED_BONUS`），同段同分时干净版必在 OCR 版前。实测两对同段 6 组对决 5 组干净版胜，唯一 OCR 胜出是词频真实更高（8:6），契约内正当 | （原风险）新手把「汶个核心票」错字当原文引用 | 采纳审计建议的排序权重方案，未动导入数据。两源分块边界不同、无逐块 twin，彻底去重需显示层折叠，暂不做 |
| P2 | 方法卡数量少（约 20 张） | 蒸馏成果不足 | 批量运行蒸馏管道，生成草稿队列 |

---

## 不做的事

- 不执行自动交易，不连接券商
- 不生成荐股结论或收益保证
- 不把作者历史观点当作当前市场规律
- 不把作者案例改写成用户自己的交易经历
- 不在没有评测证据的情况下引入向量数据库
- 不在未 dry-run 的情况下直接导入新来源
