# 交易知识库

> 项目根目录：`C:\Users\20577\Documents\炒股\知识库`
> 索引数据库：`_知识库系统\indexes\knowledge.db`
> 最后更新：2026-08-04（接入第 5 个来源 `aizaibingchuan`，当日补齐其 2023 三季度空洞）

---

## 这个系统在做什么

把交易员的经验资料（直播转写、方法论文章、付费课程）蒸馏成可执行的判断。

**核心分工：**
- AI：扫描原始内容、初步判断价值、提炼草稿
- 用户：确认 / 修改 / 拒绝草稿 → 写入方法卡（status: reviewed）

**核心指标是方法卡数量，不是可检索记录数。**
一张用户审批过的方法卡 = 真正"摘抄进了笔记本"。

---

## 目录结构

```
知识库\
├── 复利杯\                        ← 原始资料，只读
├── 南京路彼岸\                    ← 原始资料，只读
├── 郁金香花园付费文章文档版\      ← 原始资料，只读
├── 飞书聊天记录_潘凤\             ← 原始资料，只读（已 gitignore，体积大）
├── 爱在冰川\                      ← 原始资料，只读（2655 个 .md，87 MB）
├── .agents\skills\                ← 五个项目级工作流（见下方 Skills）
└── _知识库系统\
    ├── config\                    ← sources.yaml / taxonomy.yaml
    ├── source_libraries\          ← 每个正式来源一个目录，目录名 = source_id
    ├── indexes\knowledge.db       ← SQLite FTS5
    ├── personal\trading_journal\  ← 个人日志（私密，不混入来源）
    └── scripts\                   ← 导入/索引/查询/验证脚本
```

**只读原则：原始来源文件夹内的文件不得覆盖、改名、移动或写入派生内容。**

**来源清单以 `_知识库系统\config\sources.yaml` 为唯一事实来源。** 上面的目录树会过时，
登记表不会。原始目录名不必等于 `source_id`（`panfeng` 的原始目录是
`飞书聊天记录_潘凤\`，载体名保留原样），但 `source_libraries\` 下的目录名必须等于
`source_id`——`build_index.py` 靠这个识别来源。

---

## Skills（工作流入口）

| 场景 | 触发方式 |
|------|---------|
| 学习一个概念，生成方法卡草稿 | `/trading-knowledge-tutor` |
| 跨来源对比同一概念 | `/cross-source-synthesizer` |
| 核验某个观点是否有当前市场证据 | `/market-evidence-verifier` |
| 查行情该调哪个接口（盘中实时 + 盘后复盘） | `/market-data-toolbox` |
| 复盘一笔交易，对照方法卡 | `/trading-journal-reviewer` |
| 导入新来源，执行预检和注册 | `/trading-source-curator` |

---

## 当前已接入的来源

| id | display_name | 块数 | 载体 | 批准日期 |
|----|--------------|------|------|---------|
| `aizaibingchuan` | 爱在冰川 | 5813 | 微信公众号 Markdown + 9 篇 PDF/docx 汇编 | 2026-08-04 |
| `fulibei` | 复利杯 | 1470 | 语音转写文字稿 | — |
| `nanjinglu_bian` | 南京路彼岸 | 525 | PDF 文章 | — |
| `tulip_garden` | 郁金香花园 | 1181 | Word + 截图 | — |
| `panfeng` | 我有上将潘凤 | 186 | 飞书聊天记录 HTML | 2026-08-02 |

已接入的来源**彼此同级，没有主次**。跨来源检索时逐个查询、分别记录，不合并。
新增来源走 `/trading-source-curator`，必须先 `--dry-run` 再由用户批准，
批准日期记入 `sources.yaml` 的 `approved_on`。

以下各节按来源列出各自的形态和已知问题。

### 复利杯（fulibei）

语音转写的直播/连麦文字稿，多人，有说话人和时间戳。
干货密度低，闲聊多（前 10-15 分钟属于正常）。
有价值的部分是一线交易员实战思路（闻少、曾一六等）。

**⚠️ 已知问题（截止 2026-08-01）：**
- `claim_type` 全部固定为 `opinion_or_case`，不可用于过滤
- `confidence` 全部固定为 `medium`，不可用于过滤
- 闲聊段落未标记，检索会混入无关结果
- ASR 同音字多：竞价/竟价，顺灏/顺浩同学

---

### 南京路彼岸（nanjinglu_bian）

单作者连续方法论文章，体系最完整，是概念学习的主干来源。
"弱转强"、"情绪周期"、"趋势对抗"等核心概念有完整推演。

**✅ 抽取方式已修复（2026-08-02）：改为文本层 + OCR 双路保留**

这批 PDF 是「文章正文（有文本层）+ 嵌入行情截图（只能 OCR）」的混合页，
择一使用必然丢内容。实测「文本层优先，择一」方案会让 75 页正文减少约 1 万字，
因为截图内容被丢掉了。现在同一页产出两类块，各自标注可信度：

| chunk_type | confidence | 块数 | 内容 |
|-----------|-----------|-----|------|
| `article` | high | 238 | 文本层正文，干净（cjk 中位 0.867） |
| `article` | medium | 101 | 无可用文本层，OCR 承载正文，有误识 |
| `screenshot_ocr` | low | 186 | 截图内容，误识较多，仅此层可得 |

中文字符总量 125635 → 144576。回答时优先用 `high`，
引用 `low` 块必须标注 `⚠️ OCR低置信度` 并建议回看原页。

OCR 重复识别的正文已用 n-gram 比对剔除，不会与文本层重复。

---

### 郁金香花园（tulip_garden）

付费课程，Word 正文 + 大量截图，涵盖筹码、竞价、仓位。

**✅ 切块过碎已修复（2026-08-02）：**
连续短段合并为 400-800 字的块，图片 OCR 块保持独立。

| 指标 | 修复前 | 修复后 |
|------|-------|-------|
| 块数 | 6393 | 1181 |
| 中位数 | 19 字 | 410 字 |
| 短于 50 字 | 77.8% | 6.9% |

内容逐字符校验无丢失（430541 字符）。合并块的 `locator` 是段落区间
（如 `正文第49段—正文第57段`），原始段号保留在 `merged_locators` 字段。

剩余 6.9% 短块中，图片说明占 68%，正文列表项占 30%（被相邻截图隔断，
展开 Parent 可读到上下文）。

---

### 我有上将潘凤（panfeng）

盘中聊天记录，飞书 HTML 导出。载体是聊天记录，来源身份是作者本人——
换成微信或语音转写，来源还是同一个人。所以 `source_id` 用作者身份命名，
而 `chunk_type: feishu_chat`、`adapter: specialized_feishu_chat` 保留载体名，
描述的是结构和解析能力。

| 项 | 值 |
|---|---|
| 原始 HTML | 1 个，16 个消息组，3445 条消息 |
| 发言人 | 我有上将潘凤 3326 条、5280 共 119 条 |
| 日期范围 | 2026-06-23 至 2026-07-31，29 个有消息的交易日 |
| 产物 | 29 个每日文档 / 60 个父块 / 186 个检索块 |
| 图片、外链 | 均为 0 |
| 定位方式 | 日期 + 时间 + 消息编号（`primary_locator: date_time_message_number`） |

**⚠️ 已知特性（不是缺陷，检索时需知道）：**

- **块数少一个数量级**（186 vs 1470/525/1181），且是口语而非术语。
  实测 `弱转强` 0 条、`龙头` 2 条、`竞价` 1 条、`半路` 16 条。
  查不到很常见，如实报"未找到"，不要硬凑
- **日期部分是结构化推断**：部分组头只有时分没有年月日，导入器从绝对日期锚点
  出发、在组内时间回退 90 分钟以上时推进到下一个周一至周五（共推断 23 次）。
  规则与全部锚点闭合，但**不等同于飞书原生时间戳**，日期敏感的结论要回看原文
- **原文有规避写法**：拼音、希腊字母、同音字替代证券名称。标准化只做 Unicode、
  空白和冗余签名清理（剔除尾部 `【潘凤老师】` 1047 条），**不猜测恢复证券名称**
- **`unknown` 时间继承 2 条**：飞书导出里时间缺失的消息继承上一条的时间
- 全部内容标为历史观点或案例，不作为当前市场事实

---

### 爱在冰川（aizaibingchuan）

微信公众号导出 Markdown，单作者，2017-06 至 2026-08 跨 10 年，库内体量最大的来源
（5813 块，占全库 63.4%）。主体是每日盘后复盘与次日计划，另有周末随笔和方法论文章。
另有 9 篇第三方汇编（PDF/docx）单独入库，见下方「额外资料」小节。

| 项 | 值 |
|---|---|
| 原始 .md | 2655 个，87 MB（原始目录 `爱在冰川\`，只读） |
| 产物 | 2537 文档 / 5184 父块 / 5813 检索块 / 4,094,344 字符 |
| 块长 | 最短 35，中位 669，均值 704，最长 1601 |
| 定位方式 | 正文按段号（`正文第12段—正文第18段`），作者回复按留言序号（`留言区第1条—第8条`） |
| 跳过文档 | 127 篇整篇未入库（见下方说明，用户 2026-08-04 决定不救） |

| chunk_type | 块数 | 内容 |
|-----------|-----|------|
| `qa_reply` | 2376 | 留言区里作者本人的回复，与提问配成「问：…答：…」 |
| `daily_review` | 1653 | 每日盘后复盘 |
| `daily_data` | 944 | 每日数据小节（涨停动因、赚钱效应、各种数据） |
| `article` | 616 | 方法论文章 |
| `weekend_essay` | 175 | 周末随笔 |
| `curated_digest` | 36 | 第三方摘录/语录汇编，**不是作者原文**（见额外资料小节） |
| `course_ocr` | 13 | 付费课程讲义扫描件 OCR，误识多 |

**2023 三季度空洞已补齐（2026-08-04 增量导入）。** 用户补下载了缺失的 `.md`，
源端新增 882 个文件里按同一套规则（`azbc_delta_new.py` 复用 `azbc_pick.py` 的 classify）
筛出 **71 篇入库**（每日数据 59 / 鱼块 11 / 其他正文 1），剔除资讯汇总 621 + 资金流 107
+ 无正文 83。产物增量：documents +71、parents +79、chunks +131，新增块噪声检查
（图片语法/裸链接/粗体/读者留言/免责行/表情占位符/locator 格式）**全部 0 异常**。

补完后 2023-07-01~09-30 的 65 个工作日里只剩 7 天无内容（`07-06`、`07-13`、`07-19`、
`09-04`、`09-19`、`09-27`、`09-29`，末者中秋休市），作者本就不是每个交易日都发文，
**不再算空洞**。全时段剩余 22 段连续空白（≥3 工作日）逐段核对为国庆/春节/劳动节/中秋，
真正非节假日的只剩 `2017-07-14~19`、`2024-06-25~27`、`2024-10-18~22` 三段。

**127 篇跳过文档到底是什么**（2026-08-04 实测更正，此前文档写成「只留作者回复」是错的）：

跳过条件在 `scripts\import_aizaibingchuan.py:575`：正文清洗后中文不足 100 字
**且**留言区无作者回复 —— 两个条件同时成立才跳过，且是**整篇跳过、连 document 都不生成**。
实测 `documents.jsonl` 2537 篇全部有 chunk，零 chunk 文档 0 篇，印证了这一点。

「靠作者回复入库」是**另外 97 篇**（正文<100 字但 `author_reply_count`>0），
这 97 篇全部入库了。两者是不同的集合，别混为一谈。
127 篇的逐篇清单见 `爱在冰川数据导入任务包\06_待你确认_127篇清单.md`
（A 组 30 篇作者实写 ≥60 字、B 组 31 篇 20-59 字、C 组 66 篇 <20 字）。

**用户 2026-08-04 决定：不救。** `MIN_DOC_CHARS` 保持 100 不下调。
所以这 127 篇的内容永久不在库内，问到对应日期会「查不到」——
其中 30 篇作者实写 ≥60 字（如 `2024-08-28 数据`），需要时只能回看原文。
2023 三季度增量导入后跳过数仍是 127，说明新补的 71 篇一篇都没落进跳过条件。

**有意丢弃的内容：**

- 读者留言 8,001,028 字符全部丢弃，只提作者本人回复（785,523 字符，14,136 条）
- 资讯汇总 621 篇、短线资金流 107 篇、无正文 83 篇未纳入（用户明确要求），合计剔除 811 篇。
  验算：源端 3466 个 `.md` = 入库 2655 + 剔除 811
- 微信样板、图片语法、裸链接、表情占位符共占原文约 77%，已清洗

**⚠️ 已知问题（截止 2026-08-04）：**

- **公众号正文里的图片未 OCR**：原文大量行情截图、持仓截图，图表内容一律不在库内。
  `risk_flags` 已标注，结论涉及图表必须回看原文。
  （例外：额外资料里的课程讲义做了 OCR，见下方小节）
- **`date` 是发布日，不是交易日**：取自文件名方括号。2026-08-04 增量导入后重测
  （`azbc_recount3.py`，规则：标题里 `20YY`+2~4 位数字能**唯一**切出合法日期才计，
  切分有歧义或一个标题里出现多个日期都不计）：2528 篇 `.md` 里可判定 1855 篇、
  歧义 210 篇、无日期 463 篇。可判定的 1855 篇中 **1496 篇与发布日同日、
  323 篇发布日晚 1 天**（盘后复盘发在次日凌晨）、14 篇晚 2 天、
  10 篇差 89~1096 天（**全是作者把年份打错**，如《2018年的第一个交易日201711爱在冰川复盘》
  发布于 2018-01-01 而标题写成 2017）。日期敏感的结论要核对标题里的日期。
  这批数字比旧版记录的「402 篇带交易日」多，因为旧版正则窄，漏掉了 `202372数据`
  这类无分隔符写法，不是数据变了
- **作者对持仓打码**：原文按「阅读上 2w 就打马赛克」的惯例隐去证券名，
  原文里就已缺失，库内同样没有，**不猜测恢复**
- `claim_type` 全部固定为 `opinion_or_case`（5813 块）、`market_regime` 全部「未标注」，
  两者都是导入器写死的常量，不可用于过滤
- `confidence` 只在额外资料上有区分：公众号正文 5764 块全为 `high`（写死的常量，
  **不代表评估结果**），额外资料 36 块 `medium` + 13 块 `low`（按素材性质实标）。
  换句话说，拿 `confidence=high` 筛这个来源等于筛掉那 49 块第三方汇编，筛不出"更可靠的正文"
- `topics` 有约 1.4% 的块为空，属词表未命中，与 panfeng 的 42.5% 同类，不是缺陷

**该来源独有说法**（其他四来源零命中，检索时单独试，括号为 2026-08-04 增量导入后重测块数）：
`涨停动因` 1354、`各种数据` 1420、`方向展望` 385、`北向资金` 267、`填权` 178、
`业绩报` 151、`川神` 709、`股池` 5、`长上影` 20、`瞎扯淡` 181、
`极度低迷` 3、`空间龙` 5、`无人关注` 1。
另有 `借楼` 963 —— 这是作者在留言区回复的固定开头（「答：借楼一用：…」），
是载体特征不是交易术语，用它检索等于筛出全部留言回复。

#### 额外资料（9 篇第三方汇编，49 块）

不是公众号导出，是另外收来的 PDF/docx，放在 `爱在冰川\额外资料\`（只读），
由 `scripts\import_azbc_extras.py` 单独导入（重跑幂等）。
**其中 8 篇不是作者本人发布的内容**，`author` 字段已写明整理者，
检索结果里必须按 `author` 区分，不能当成导师原话引用。

| 素材 | 篇 | 块 | chunk_type | confidence | author |
|------|---|---|-----------|-----------|--------|
| 干货文章点集（股不南摘录） | 7 | 31 | `curated_digest` | medium | 股不南（读者摘录，非作者原文） |
| 语录及感悟（STALK 整理） | 1 | 5 | `curated_digest` | medium | STALK 整理（非作者原文排版） |
| 高送转战法付费课程讲义 | 1 | 13 | `course_ocr` | low | 爱在冰川（课程讲义，OCR） |

- 日期：干货点取文件名前缀（2023-06-22 ~ 2023-10-13）、课程讲义取 PDF `/CreationDate`
  （2018-11-09）、语录取 docx `created`（2023-09-21）。**都不是交易日**
- 干货点与库内原文有 3-8 成重复（探针实测每篇 40 条抽样命中 7~30 条），
  检索时可能同一观点出现两次，一次是原文一次是摘录
- **课程讲义 OCR 全 24 页收录，只丢了末页**（中文不足 40 字）。
  **没有按「正文页 / 行情截图页」分流** —— 试过六个指标（中文率、数字率、英文率、
  句读密度、常用字率、最长连续中文串），两类页面取值区间全部重叠，最优组合也只有
  「正文留 6/11、截图误留 3、正文误删 5」。根因是截图页 OCR 出的不是垃圾：K 线批注、
  行情表里的股票名都是真内容。所以同一块里可能正文与乱码混排，
  例如 `与其代码相近的东音股份（002793），开盘顶一反抢筹。后续涨幅最大 38%` 之后
  紧跟 `蘸 2 0 5 % ) 肝 2 ( 3 1 3 的 %`。**数字（价格、日期、涨幅）尤其不可信**，
  任何结论必须回看原页
- `source_summary.json` 由主导入器写、只统计 .md 那批，额外资料的总数补记在
  该文件的 `extras_included` 键里。**重跑 `import_aizaibingchuan.py` 会整文件覆盖
  jsonl 并抹掉这 49 行，之后必须再跑一次 `import_azbc_extras.py`**

---

## 数据层级与可信度

```
原始文件（只读）→ Parent（上下文单元）→ Chunk（检索定位）→ 方法卡（核心产出）
```

| 层级 | 可信度说明 |
|------|-----------|
| 原始文件 | 最高，需人工阅读 |
| Parent | 高，回答的主要依据 |
| Chunk | 仅用于检索定位，不能只看摘要 |
| 方法卡 status=reviewed | 高，用户已确认 |
| 方法卡 status=draft | 草稿，待确认 |
| claim_type 字段 | **不可信**，各来源均为固定值（全库 9175 块都是 `opinion_or_case`） |
| confidence 字段 | 只有南京路、郁金香可信；`aizaibingchuan` 正文部分与 `panfeng` 固定 `high`，`fulibei` 固定 `medium` |

**`confidence=high` 不等于高可信。** 2026-08-04 增量导入后实测取值分布（全库 9175 块）：

| source_id | high | medium | low | 其他 | 是否有真实区分 |
|-----------|------|--------|-----|------|--------------|
| `nanjinglu_bian` | 238 | 101 | 186 | — | 有 |
| `tulip_garden` | 507 | 674 | — | — | 有 |
| `aizaibingchuan` | 5764 | 36 | 13 | — | 仅额外资料那 49 块有；正文 5764 块是写死的 |
| `panfeng` | 186 | — | — | — | 无，写死 |
| `fulibei` | — | 1444 | — | curated 26 | 无，写死 |

跨来源按 `confidence` 排序会把爱在冰川的 5764 块正文整体顶到最前，这是假象。

**南京路检索优先级：** `chunk_type=article` + `confidence=high`（文本层正文）
优先于 `medium`（OCR 正文）优先于 `screenshot_ocr`（截图，须标注低置信度）。

---

## 常用同义词（检索时扩展）

括号内是**实测命中块数**（2026-08-04 增量导入后对 knowledge.db 重测，`text` 列 GLOB 子串匹配）。
全库 9175 块（爱在冰川 5813 + 复利杯 1470 + 郁金香 1181 + 南京路 525 + 潘凤 186）。
数字小的词照样要查 —— 低频不等于无效，`先于龙` 只有 9 块但 7 块在南京路，是该来源的独特说法。

| 概念 | 各来源常见写法 |
|------|--------------|
| 情绪周期（319） | 冰点（485）、高潮（491）、分歧（573）、退潮（269）、情绪冰点（65） |
| 弱转强（263） | 转势（54）、强转弱（6）、走弱转强（1，复利杯独有） |
| 龙头（1511） | 龙一（45）、龙二（45）、核心票（27）、先于龙（9，主要在南京路）、人气核心（2） |
| 主力（190） | 机构（1086）、散户（405）、游资（298）、大资金（259）、北向资金（267，冰川独有）、外资（195）、庄家（31） |
| 入场（169） | 打板（1350）、低吸（1228）、潜伏（573）、开仓（455）、追高（325）、半路（211）、埋伏（154）、进场（136）、起步（50） |
| 竞价（1341） | 高开（778）、低开（651）、集合竞价（217）、竟价（129，错别字，**128 在郁金香**）、竞价抢筹（46） |
| 连板（946） | 反包（768）、首板（719）、二板（361）、三板（256）、烂板（212）、断板（180）、板高（43）、连板高度（15） |
| 涨停归因 | 涨停动因（1354，**冰川独有**）、动因（1365）、涨停原因（9） |
| 卖出与下跌 | 割肉（842）、杀跌（834）、利空（560）、止损（335）、跳水（258）、砸盘（161）、补跌（113） |
| 仓位（986） | 空仓（454）、加仓（363）、满仓（298）、清仓（205）、减仓（201）、半仓（130）、底仓（56） |
| 缩量放量 | 缩量（1129）、放量（793）、均线（283）、影线（247）、上影线（136）、十字星（91）、长上影（20，冰川独有） |
| 高送转 | 送转（528）、高送转（427）、填权（178，冰川独有）、业绩报（151，冰川独有）、除权（92）、预增（76） |
| 自选池 | 票池（297）、自选股（49）、股池（5，冰川独有）、观察池（2，郁金香独有） |
| 监管异动 | 异动（381）、监管（360）、特停（124，122 在冰川）、卡异动（3）、被关小黑屋（2，复利杯独有） |

**「入场」上一版写「无此词」是错的，实测 169 块。** 旧版大概是把它当纯概念名没测，
这次逐词重跑（`azbc_recount3.py`）确认冰川 157 / 复利 6 / 郁金 4 / 南京 2。

**死条目会被新来源复活。** 上一版标注「全库 0 命中」而删掉的 `极度低迷`、`空间龙`，
爱在冰川接入后分别有 3 块和 5 块（全在冰川）；`无人关注` 这次也复活了 1 块（在冰川）。
仍为 0 的只剩：`市场最悲观`、`由弱转强`、`先弱后强`、`入场时机`。
每次接入新来源或做增量导入都要重测一遍，不能沿用旧判断。

**同义词不能反向替换。** `弱转强` 在复利杯有 43 块，但 `转势` 在复利杯**零命中** ——
扩展只能加词，不能拿同义词顶替原词。同理 `北向资金`、`涨停动因`、`填权` 只在爱在冰川
出现，拿它们检索等于只查一个来源。

**高送转类词现在会捞到课程讲义 OCR。** `高送转` 427 块里有 10 块、`送转` 528 块里有
11 块、`除权` 92 块里有 4 块来自 `chunk_type=course_ocr`（`confidence=low`，
正文与 K 线乱码混排）。这批是全库唯一系统讲高送转打法的素材，值得看，
但**里面的价格、日期、涨幅数字不可信**，引用前必须回看原页。
另有第三方摘录 `curated_digest` 也会被捞到（`龙头` 19 块、`低吸` 17 块、
`送转` 3 块、`高送转` 2 块、`填权` 1 块），那不是作者原文，按 `author` 区分。

**载体特征词不是交易术语。** `借楼`（963 块）是爱在冰川留言区回复的固定开头
（「答：借楼一用：…」），`各种数据`（1420）、`方向展望`（385）、`瞎扯淡`（181）
是该来源的固定小节名。用它们检索筛出的是结构位置，不是观点。

**同一个词在不同来源可能指不同概念**，归一前先看该来源自己的定义。
实例：「容量」在南京路指市场资金容量，在郁金香指个股换手承接能力。

---

## AI 工作规则

**先检索，再回答。** 不用模型记忆代替检索结果。

**说清用的是哪层：**
- 来自方法卡：说明 status 是否 reviewed
- 来自 Chunk/Parent：给出来源定位
- 来自模型常识：明确说"这是一般性说明，不来自库内资料"

**引用格式：**
```
[来源 | 作者/嘉宾 | 文档 | 日期 | 页码/时间戳]
```
OCR 不确定时标注 `⚠️ OCR低置信度`，说话人不确定时标注 `⚠️ 说话人待核实`。

**历史观点 ≠ 当前市场事实。**
问题含"今天、现在、目前、是否仍有效"时：库内资料只说"作者历史上怎么说"；当前事实通过 Web/MCP 核验；两者分开写，注明截止日期。

**行情数据统一走 `live-market` MCP**（自建，29 个工具，短线 + 盘中 + 盘后全覆盖）。
脚本 `live_market.py`（实时基础）+ `live_market_ex.py`（股池/历史分钟/资金流/盘后）
+ `live_market_ths.py`（短线专用：涨停归因/连板天梯/题材聚集度）。
东财 push2delay / push2ex / datacenter-web + 同花顺 data.10jqka + 腾讯 + 新浪
多源自动降级，免费无需注册，延迟 1-3 秒。

调用顺序：
1. **短线看盘先 `get_shortline_board`**（题材主线 + 连板天梯 + 归因热词，约 2KB）
2. `get_market_temperature` 定位情绪周期
3. 封板结构看 `get_zt_pool_detail`（带首封时间、炸板次数、封单额、封成比）
4. 「为什么涨停」看 `get_stock_reason`（同花顺归因全文，行业原因 + 公司原因）
5. 弱转强/反包看 `get_yesterday_zt`（昨涨停今日表现 + 红盘率）
6. 冲高回落看 `get_intraday_shape`（量化成数字，可回溯约 60 个交易日）
7. 盘后复盘看 `get_after_close`（一次取齐 + 自动算封板成功率、炸板率）

**两套涨停数据分工，别重复调：** 东财 `get_zt_pool_detail` 给钱的维度（封单额、封成比），
同花顺 `get_zt_reason` 给逻辑的维度（涨停归因标签、官方封板成功率）。
**「连板数」两家口径不同**：同花顺「3天2板」是最近 3 日涨停 2 次（不要求连续），
东财「连板数」是末尾连续涨停天数。都对，问的不是同一个问题，别互相印证。

完整接口对照表、判读口径、数据发布时点、已探测确认不可用的源见 `/market-data-toolbox`。
第三方 `stock_data` MCP 只用筹码 `stock_chip`、历史日频资金流 `stock_fund_flow`、
所属板块 `stock_sector_spot`、快讯 `stock_news_global` 这几个；实时行情类已熔断，
`akshare_one` 整体不可用，别调。

**`akshare` 本机可用**（1.17.80 已装），但必须在 `import akshare` **之前** pop 掉
proxy 环境变量并设 `NO_PROXY="*"` —— `trust_env=False` 管不到库内部自建的 session。
上一版记「akshare 全挂」是误判。它能补的是我现在没有的：营业部排行（带上榜后胜率）、
业绩预告、高送转、大单追踪、停复牌。**没接进 MCP**（同步阻塞、部分接口本机不通），
要用就在脚本里临时 import。

**自己归纳字段含义之前先翻现成库的源码。** 2026-08-04 实证：盘口异动 22 个 type 码，
我在东财页面和 13 个 JS 里两轮 0 命中后按实测特征归纳，结果 **15 个名字是错的**；
而 akshare 源码 `stock_pankou_em.py` 里就有官方 `symbol_map`。已改用官方映射。

**盘后数据有发布时点，查不到先看时点再怀疑接口。** 龙虎榜和大宗交易盘后才出，
盘中查当天返回 `数据状态: 尚未发布` 是正常的；两融是 T+1。
取数失败就说失败，不用模型记忆里的行情假装是实时数据，也不拿前一天的数据冒充当天。

**来源之间有分歧时并列呈现，不合并。找不到证据就说找不到。**

---

## 待修复事项

| 优先级 | 问题 | 影响 | 方案 |
|--------|------|------|------|
| ~~P0~~ | ~~南京路：有文本层的页面被错误 OCR~~ | 已修复 2026-08-02 | 双路保留，见上方小节 |
| ~~P0~~ | ~~郁金香：切块过碎~~ | 已修复 2026-08-02 | 见上方郁金香小节 |
| P0 | FTS trigram 无法匹配两字词 | `竞价`/`筹码`/`龙头` 直接检索 0 命中，只能走 LIKE 兜底 | 见 SPEC 阶段 2：改走 GLOB，不换分词器 |
| P0 | LIKE 兜底候选池按 rowid 截断 | **2026-08-04 增量后复测仍是最严重的可用性问题**：爱在冰川占 rowid 1–5813（全库 63.4%），`竞价`/`龙头`/`弱转强`/`筹码`/`情绪周期` 前 50 条命中 **100% 全是爱在冰川**，其余四来源整体缺席 | 见 SPEC 阶段 2；跨来源检索必须逐来源加 `source_id` 过滤单独查 |
| P0 | `build_index.py` 不按 `sources.yaml` 的 `status` 过滤 | 未审批来源落盘即自动进索引 | 建索引前读登记表 |
| P1 | 各来源 claim_type 均为固定值 | 元数据过滤失效 | 规则+LLM 重新标注 |
| P1 | 复利杯闲聊未标记 | 检索结果夹杂无关内容 | 识别并标记 chunk_type=chitchat |
| ~~P1~~ | ~~爱在冰川 2023 三季度时间空洞~~ | 已补齐 2026-08-04 | 用户补下载 `.md`，增量导入 71 篇（+131 块）。补完后该季度 65 个工作日只剩 7 天无内容，属作者未发文，见上方小节 |
| ~~P1~~ | ~~爱在冰川 127 篇整篇未入库~~ | **用户 2026-08-04 决定不救** | `MIN_DOC_CHARS` 保持 100。这 127 篇内容永久不在库内，其中 30 篇作者实写 ≥60 字，需要时回看原文。清单留档在 `06_待你确认_127篇清单.md` |
| P2 | 干货点摘录与库内原文 3-8 成重复 | 同一观点可能检索到两次（一次原文、一次摘录），计数会虚高 | 已用 `author` 字段区分；如需去重再做 n-gram 比对 |
| P2 | 方法卡数量少（约 20 张） | 蒸馏成果不足 | 批量运行蒸馏管道，生成草稿队列 |

---

## 不做的事

- 不执行自动交易，不连接券商
- 不生成荐股结论或收益保证
- 不把作者历史观点当作当前市场规律
- 不把作者案例改写成用户自己的交易经历
- 不在没有评测证据的情况下引入向量数据库
- 不在未 dry-run 的情况下直接导入新来源
