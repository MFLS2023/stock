# 交易知识库

> 项目根目录：`C:\Users\20577\Documents\炒股\知识库`
> 索引数据库：`_知识库系统\indexes\knowledge.db`
> 最后更新：2026-08-01

---

## 这个系统在做什么

把交易员的经验资料（直播转写、方法论文章、付费课程）蒸馏成可执行的判断。

**核心分工：**
- AI：扫描原始内容、初步判断价值、提炼草稿
- 用户：确认 / 修改 / 拒绝草稿 → 写入方法卡（status: reviewed）

**核心指标是方法卡数量，不是可检索记录数。**
一张用户审批过的方法卡 = 真正"摘抄进了笔记本"。

---

## 目录结构

```
知识库\
├── 复利杯\                        ← 原始资料，只读
├── 南京路彼岸\                    ← 原始资料，只读
├── 郁金香花园付费文章文档版\      ← 原始资料，只读
├── .agents\skills\                ← 五个项目级工作流（见下方 Skills）
└── _知识库系统\
    ├── config\                    ← sources.yaml / taxonomy.yaml
    ├── source_libraries\          ← fulibei / nanjinglu_bian / tulip_garden
    ├── indexes\knowledge.db       ← SQLite FTS5
    ├── personal\trading_journal\  ← 个人日志（私密，不混入来源）
    └── scripts\                   ← 导入/索引/查询/验证脚本
```

**只读原则：原始来源文件夹内的文件不得覆盖、改名、移动或写入派生内容。**

---

## Skills（工作流入口）

| 场景 | 触发方式 |
|------|---------|
| 学习一个概念，生成方法卡草稿 | `/trading-knowledge-tutor` |
| 跨来源对比同一概念 | `/cross-source-synthesizer` |
| 核验某个观点是否有当前市场证据 | `/market-evidence-verifier` |
| 复盘一笔交易，对照方法卡 | `/trading-journal-reviewer` |
| 导入新来源，执行预检和注册 | `/trading-source-curator` |

---

## 三个来源

### 复利杯（fulibei）

语音转写的直播/连麦文字稿，多人，有说话人和时间戳。
干货密度低，闲聊多（前 10-15 分钟属于正常）。
有价值的部分是一线交易员实战思路（闻少、曾一六等）。

**⚠️ 已知问题（截止 2026-08-01）：**
- `claim_type` 全部固定为 `opinion_or_case`，不可用于过滤
- `confidence` 全部固定为 `medium`，不可用于过滤
- 闲聊段落未标记，检索会混入无关结果
- ASR 同音字多：竞价/竟价，顺灏/顺浩同学

---

### 南京路彼岸（nanjinglu_bian）

单作者连续方法论文章，体系最完整，是概念学习的主干来源。
"弱转强"、"情绪周期"、"趋势对抗"等核心概念有完整推演。

**✅ 抽取方式已修复（2026-08-02）：改为文本层 + OCR 双路保留**

这批 PDF 是「文章正文（有文本层）+ 嵌入行情截图（只能 OCR）」的混合页，
择一使用必然丢内容。实测「文本层优先，择一」方案会让 75 页正文减少约 1 万字，
因为截图内容被丢掉了。现在同一页产出两类块，各自标注可信度：

| chunk_type | confidence | 块数 | 内容 |
|-----------|-----------|-----|------|
| `article` | high | 238 | 文本层正文，干净（cjk 中位 0.867） |
| `article` | medium | 101 | 无可用文本层，OCR 承载正文，有误识 |
| `screenshot_ocr` | low | 186 | 截图内容，误识较多，仅此层可得 |

中文字符总量 125635 → 144576。回答时优先用 `high`，
引用 `low` 块必须标注 `⚠️ OCR低置信度` 并建议回看原页。

OCR 重复识别的正文已用 n-gram 比对剔除，不会与文本层重复。

---

### 郁金香花园（tulip_garden）

付费课程，Word 正文 + 大量截图，涵盖筹码、竞价、仓位。

**✅ 切块过碎已修复（2026-08-02）：**
连续短段合并为 400-800 字的块，图片 OCR 块保持独立。

| 指标 | 修复前 | 修复后 |
|------|-------|-------|
| 块数 | 6393 | 1181 |
| 中位数 | 19 字 | 410 字 |
| 短于 50 字 | 77.8% | 6.9% |

内容逐字符校验无丢失（430541 字符）。合并块的 `locator` 是段落区间
（如 `正文第49段—正文第57段`），原始段号保留在 `merged_locators` 字段。

剩余 6.9% 短块中，图片说明占 68%，正文列表项占 30%（被相邻截图隔断，
展开 Parent 可读到上下文）。

---

## 数据层级与可信度

```
原始文件（只读）→ Parent（上下文单元）→ Chunk（检索定位）→ 方法卡（核心产出）
```

| 层级 | 可信度说明 |
|------|-----------|
| 原始文件 | 最高，需人工阅读 |
| Parent | 高，回答的主要依据 |
| Chunk | 仅用于检索定位，不能只看摘要 |
| 方法卡 status=reviewed | 高，用户已确认 |
| 方法卡 status=draft | 草稿，待确认 |
| claim_type 字段 | **不可信**，三来源均为固定值 |
| confidence 字段 | 南京路可信（high/medium/low 有区分）；另两来源为固定值 |

**南京路检索优先级：** `chunk_type=article` + `confidence=high`（文本层正文）
优先于 `medium`（OCR 正文）优先于 `screenshot_ocr`（截图，须标注低置信度）。

---

## 常用同义词（检索时扩展）

| 概念 | 各来源常见写法 |
|------|--------------|
| 情绪冰点 | 极度低迷、无人关注、市场最悲观、退潮 |
| 弱转强 | 由弱转强、走弱转强、转势、先弱后强 |
| 龙头 | 核心票、人气核心、空间龙、先于龙 |
| 主力 | 游资、庄家、大资金、机构 |
| 入场时机 | 介入点、进场、买入时机、起步 |
| 竞价 | 竟价（错别字，郁金香多见）、集合竞价 |
| 连板 | 连续涨停、连板高度、板高 |
| 监管异动 | 卡异动、被关小黑屋（复利杯口语） |

---

## AI 工作规则

**先检索，再回答。** 不用模型记忆代替检索结果。

**说清用的是哪层：**
- 来自方法卡：说明 status 是否 reviewed
- 来自 Chunk/Parent：给出来源定位
- 来自模型常识：明确说"这是一般性说明，不来自库内资料"

**引用格式：**
```
[来源 | 作者/嘉宾 | 文档 | 日期 | 页码/时间戳]
```
OCR 不确定时标注 `⚠️ OCR低置信度`，说话人不确定时标注 `⚠️ 说话人待核实`。

**历史观点 ≠ 当前市场事实。**
问题含"今天、现在、目前、是否仍有效"时：库内资料只说"作者历史上怎么说"；当前事实通过 Web/MCP 核验；两者分开写，注明截止日期。

**来源之间有分歧时并列呈现，不合并。找不到证据就说找不到。**

---

## 待修复事项

| 优先级 | 问题 | 影响 | 方案 |
|--------|------|------|------|
| ~~P0~~ | ~~南京路：有文本层的页面被错误 OCR~~ | 已修复 2026-08-02 | 双路保留，见上方小节 |
| ~~P0~~ | ~~郁金香：切块过碎~~ | 已修复 2026-08-02 | 见上方郁金香小节 |
| P0 | FTS trigram 无法匹配两字词 | `竞价`/`筹码`/`龙头` 直接检索 0 命中 | 改分词器或查询时补齐 3 字 |
| P1 | 三来源 claim_type 固定值 | 元数据过滤失效 | 规则+LLM 重新标注 |
| P1 | 复利杯闲聊未标记 | 检索结果夹杂无关内容 | 识别并标记 chunk_type=chitchat |
| P2 | 方法卡数量少（约 20 张） | 蒸馏成果不足 | 批量运行蒸馏管道，生成草稿队列 |

---

## 不做的事

- 不执行自动交易，不连接券商
- 不生成荐股结论或收益保证
- 不把作者历史观点当作当前市场规律
- 不把作者案例改写成用户自己的交易经历
- 不在没有评测证据的情况下引入向量数据库
- 不在未 dry-run 的情况下直接导入新来源
