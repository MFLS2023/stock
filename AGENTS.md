# 交易知识库项目规则

## 项目目的

本项目用于整理、检索、比较和学习多来源交易资料。它是研究与复盘工具，不提供自动交易、确定性荐股或收益保证。

## 目录约定

- 各来源的原始资料目录：只读。不得覆盖、改名、移动或写入派生文件。清单以 `_知识库系统/config/sources.yaml` 的 `source_path` 为准，当前为 `复利杯/`、`南京路彼岸/`、`郁金香花园付费文章文档版/`、`飞书聊天记录_潘凤/`。新增来源自动适用本条。
- `_知识库系统/config/sources.yaml`：来源登记表，**来源清单的唯一事实来源**。任何"有哪些来源"的判断都读它，不要读文档里的列表（会过时）。
- `_知识库系统/source_libraries/`：各来源的结构化知识库，子目录名 = `source_id`。原始目录名可以不同（`panfeng` 的原始目录是 `飞书聊天记录_潘凤/`，载体名保留原样）。
- `_知识库系统/cross_source/`：跨来源主题、共识、分歧和证据卡。
- `_知识库系统/personal/trading_journal/`：用户个人交易日志，和外部资料隔离。
- `_知识库系统/indexes/knowledge.db`：本地全文和元数据索引。
- `_知识库系统/evals/`：固定测试题和验证记录。
- `.agents/skills/`：项目级可复用工作流。
- `.Codex/`：实施、审查和决策留痕。

## 默认回答流程

1. 先判断问题属于：单一来源、跨来源、当前市场核查、个人交易复盘，还是一般方法学习。
2. 优先运行本地检索：

   ```powershell
   python _知识库系统/scripts/query_kb.py "问题关键词" --limit 8
   ```

3. 命中后读取完整父块或原文上下文，不只依赖搜索摘要。
4. 区分：事实、作者观点、经验规则、历史案例、猜测。
5. 历史资料不得被表述为当前事实。问题含“今天、现在、最新、目前、还能不能用”时，调用 Web/MCP/公开披露核查，并注明数据截止日期。
6. 来源之间有冲突时并列呈现，不擅自合成一个平均结论。
7. 没有找到指定来源证据时明确说未找到，不得把模型常识伪装成作者观点。

## 引用格式

重要结论必须附：

`[来源｜作者或嘉宾｜文档｜日期｜页码/时间戳/章节/图片名]`

引用必须能回到 `source_libraries` 中的结构化片段或原始文件。OCR 或说话人识别置信度较低时一并提示。

## 数值与风险

- 涨跌幅、胜率、盈亏比、回撤、仓位和统计结果使用程序或原始数据计算，不让语言模型心算关键数字。
- 对方法给出适用前提、失效条件、反例和待验证项。
- 不执行真实交易，不生成保证性语言。

## 更新与验证

接入或更新资料时依次运行：

```powershell
python _知识库系统/scripts/build_manifest.py
python _知识库系统/scripts/build_index.py
python _知识库系统/scripts/build_cross_source.py
python _知识库系统/scripts/validate_kb.py
```

只处理 manifest 中新增或变更的文件。完成标准是：原始文件未改动、来源元数据完整、引用可定位、索引可查询、验证报告通过。

新增来源时先运行 `register_source.py --dry-run` 做格式和结构盘点。常规 MD/TXT/PDF/DOCX/图片使用 `generic_mixed`；说话人、时间戳、连续截图、复杂图文关系或数字密集图表使用专用适配器，不得静默丢失结构。

**接入新来源必须先经用户批准，批准日期记入 `sources.yaml` 的 `approved_on`。**
`build_index.py` 目前无条件扫描 `source_libraries/` 下所有目录、不看 `status`，
所以产物一落盘就会自动进索引——这是已知缺陷（见 SPEC 第 5 节），不能当作默许。
`source_id` 用**来源身份**命名（作者、机构），不用资料载体命名：换个载体来源还是同一个人。
描述结构和解析能力的字段（`source_type`、`chunk_type`、`adapter`）保留载体名，便于其他来源复用适配器。
