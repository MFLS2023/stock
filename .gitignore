# ============================================
# 交易知识库 .gitignore
# 原则：跟踪代码、配置、文档和可读的派生文本；
#       排除原始资料、二进制索引和可重新生成的大件。
# ============================================

# --- 原始来源资料（只读，体积 625M，不入版本控制）---
# 每接入一个来源，把它的原始目录加进来。
# 结构化产物 source_libraries/<source_id>/ 不忽略——那是要跟踪的派生文本。
复利杯/
南京路彼岸/
郁金香花园付费文章文档版/
飞书聊天记录_潘凤/
爱在冰川/

# --- 二进制索引（48M，每次重建全量变化）---
# 回滚依赖 backups/ 下的物理备份，不依赖 Git
_知识库系统/indexes/*.db
_知识库系统/indexes/*.db-shm
_知识库系统/indexes/*.db-wal
# 导入前的库快照（如 knowledge.db.backup-before-azbc，44M），只留本机
_知识库系统/indexes/*.db.backup-*

# --- 导入前的 jsonl 快照（每次导入都会生成，爱在冰川一家就 61M）---
# 命名形如 chunks.jsonl.bak-before-extras / parents.jsonl.bak-before-delta-2023q3
# 与 .db.backup-* 同理：回滚靠本机文件，不靠 Git
_知识库系统/source_libraries/*/*.jsonl.bak-*
_知识库系统/source_libraries/*/*.jsonl.bak

# --- 可由原始资料重新生成的大件 ---
_知识库系统/source_libraries/*/assets/
_知识库系统/source_libraries/*/converted_docx/
_知识库系统/source_libraries/*/image_ocr_cache/

# --- 临时与调试产物 ---
_知识库系统/tmp/
# akshare 接口探测草稿（判断哪些第三方接口可用时的临时脚本，非项目产物）
_知识库系统/scripts/_ak.txt
_知识库系统/scripts/_ak1.py
_知识库系统/scripts/_ak_probe.py
**/__pycache__/
*.pyc
*.pyo

# --- 个人日志（私密，不进版本控制）---
_知识库系统/personal/

# --- 本地备份目录（体积大，仅本机保留）---
backups/

# --- 编辑器与系统垃圾文件 ---
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# --- 编码损坏的备份文件 ---
*.invalid-encoding.bak
